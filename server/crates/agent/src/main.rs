//! PC 側エージェントの実行ファイル（セルフホスト化設計§1-1）。
//!
//! セルフホストモードでは、PTY・フック受信・パース・自己修復はこのプロセスが持ち、
//! ダッシュボードサーバへは A2S（WebSocket）で繋ぐ。**その本体はフェーズ3 で入る。**
//!
//! フェーズ1 の時点でこの実行ファイルを置いてあるのは、**依存の境界を先に立てる**ため。
//! ここが `rust-embed` などサーバ側の荷物を引き込んでいないことを
//! `crates/core/tests/dependencies.rs` が機械で検査しており、後から配布物を軽くしようと
//! するより、最初から重くしない方が確実に守れる。
//!
//! ローカルモード（1台で完結する現行の使い方）は `agentdashboard` の方を使う。

fn main() {
    eprintln!(
        "agentdashboard-agent は未実装です（セルフホスト化フェーズ3 で実装）。\n\
         1台で使う場合は agentdashboard を起動してください。"
    );
    std::process::exit(1);
}
