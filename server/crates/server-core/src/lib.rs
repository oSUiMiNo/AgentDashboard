//! ダッシュボードサーバ側のロジック（セルフホスト化設計§2-1）。
//!
//! ブラウザとの WebSocket・REST・web アセットの配信を持つ。**PTY には触らない**のが
//! このクレートの存在理由で、PC 側へは境界 trait 越しにしか触れない。
//!
//! ローカルモードでは [`agent_core`] と同じプロセスに同居し、境界はプロセス内の直結に
//! なる。セルフホストモードでは同じ境界の向こうが A2S（WebSocket）越しのエージェントに
//! 変わる（設計§2-3）。**どちらのモードでもブラウザから見た口は変わらない。**
//!
//! フェーズ1（crate 再編成）の時点では中身の移設はまだ行っていない。

// 移設はフェーズ1 の後続コミットで行う（計画.md フェーズ1）。
