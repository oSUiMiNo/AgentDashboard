//! web のビルド成果物が変わったら core を作り直させる。
//!
//! rust-embed は `web/dist` を**コンパイル時に**読み込むが、cargo はその依存関係を
//! 自動では知らない。これが無いと、フロントエンドを直しても古い画面が同梱されたままの
//! バイナリが出来上がり、「直したはずの不具合が再現し続ける」という追いにくい状態になる。

fn main() {
    println!("cargo:rerun-if-changed=../../../web/dist");
}
