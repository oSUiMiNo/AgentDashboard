//! JSONL トランスクリプトを正規化イベントモデル（[`protocol::TreeNode`]）へ変換する。
//!
//! **このクレートは自己修復機構（設計§9）が唯一書き換えてよい範囲**。core と protocol には
//! 触れさせないことで、フォーマット変更への自動対応の爆発半径を物理的に限定している。
//! 未知の構造に出会ったら必ず [`protocol::Node::Unknown`] へ写像し、共有境界である
//! protocol クレートの変更を要求しないこと。
//!
//! 実装はフェーズ3（M3: 構造化ビュー）で行う。ここでは設計§2/§8 が定めるモジュール構成だけを
//! 先に用意し、どの責務がどこに載るかを明示しておく。

/// notify によるファイル監視と、オフセット管理による増分 tail（設計§8）。
///
/// ファイル未存在はエラーではなく「未作成」として扱う。JSONL は結果整合のチャネルであり、
/// セッション開始直後には存在しないことが実機検証で確認されている。
pub mod tail {}

/// JSONL の1行を `serde_json::Value` として受けてから既知構造への変換を試みる寛容パース（設計§8）。
pub mod parse {}

/// `uuid` / `parentUuid` によるツリー構築、tool_use と toolUseResult の対応付け、
/// `subagents/` のマウント、sidechain の接続を行うスレッディング層（設計§8 のパーサ中核）。
pub mod thread {}

/// 組み立てたツリーを [`protocol`] のイベントモデルへ落とし込む層（設計§3）。
pub mod normalize {}
