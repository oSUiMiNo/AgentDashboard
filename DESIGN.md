# UIデザインルール

## Entertainment-Rich Productivity UI Design System

---

<br/>
<br/>

# 0. このドキュメントの目的

このドキュメントは、本プロジェクトにおけるUIデザインの最上位ルールを定義する。

Coding Agent が以下を新規作成・修正・拡張する際は、原則として必ず本ドキュメントを参照すること。

* ファイル一覧
* フォルダ一覧
* ツリー
* サイドバー
* メニュー
* コンテキストメニュー
* ツールバー
* ボタン
* タブ
* ダイアログ
* パネル
* アイコン
* バッジ
* Empty State
* Loading
* 状態表示
* Hover / Selected / Dragging などのインタラクション
* 新規画面全般

本プロジェクトが目指すものは、次の一文で表せる。

> **実用ソフトとして長時間使える明快さを保ちながら、高品質なゲームUIのように、見る・触ること自体が少し楽しいUIを作る。**

---

## 0.1 合格条件は2つある

この文書は禁止が多いので、**禁止に触れないことが合格だと誤読されやすい**。そうではない。

| 側 | 何を決めるか | 節 |
|---|---|---|
| 天井 | やってはいけないこと | §5・§20・§33 |
| 床 | 最低限やらなければいけないこと | **§8** |

**片方だけ満たしても不合格。** 天井だけを守ると §6 の失敗（ただの暗いIDE）になり、
床だけを追うと装飾過多になる。

---

## 0.2 時間が無いときに読む順

全部読めないときは、この4つだけ読む。

1. **§6 守っただけでは合格しない** — 何を避けるための文書なのか
2. **§8 必達要素と最低数** — 最低限やること
3. **§4 参考にする方向性** — 何をもってゲーム的とするか（参考ではなく仕様）
4. **§34 レビューチェックリスト** — 出す前に数えるもの

アイコンを作るなら §16〜§22 を加える。

---

<br/>
<br/>

# 1. Design Statement

> **A practical tool that behaves like software, but feels like entertainment.**

実用ソフトとして機能しながら、
娯楽作品のように触れたくなるUIを作る。

---

<br/>
<br/>

# 2. 一文で表すデザイン方針

> **VS Code や Google Drive のような整理された実用UIを土台とし、Filled主体の強いアイコン、印象的なシルエット、Controlled Asymmetry、適度な物質感、グラフィック性、短く気持ちいいモーションを加えることで、実用性とエンタメ性を両立する。**

---

<br/>
<br/>

# 3. 目指す5要素

本プロジェクトのUIは、以下を同時に満たすことを目標とする。

## 3.1 Functional

* 迷わず使える
* 何が押せるか分かる
* 情報を素早く探せる
* 一覧性が高い

## 3.2 Readable

* 情報量が多くても読みやすい
* 長時間使っても疲れにくい
* 小さな表示でも判別しやすい

## 3.3 Graphic

* UIが単なるフォーム部品の集合に見えない
* 画面全体がグラフィックとして成立する

## 3.4 Playful

* 少しコミカル
* 少し意外性がある
* 少し触っていて気持ちいい
* 無機質すぎない

## 3.5 Premium

* テンプレート感がない
* 安っぽく見えない
* ゲーム内に出ても急に品質が落ちたように見えない

---

<br/>
<br/>

# 4. 参考にする方向性

既存作品そのものをコピーするのではなく、**原理**を参考にする。

**この節は参考ではなく、要求である。** 節名につられて読み飛ばさないこと。
本プロジェクトのゲーム性を作る指示は、事実上この節にしか書かれていない。
ここを渡さずに作ると、§6 の失敗（ただの暗いIDE）にそのまま落ちる（実測）。

## 4.1 VS Code / Google Drive から学ぶもの

* 一覧性
* 情報密度
* 高速にスキャンできる構成
* ファイル名を主役にする考え方
* 余計な重複情報を減らす姿勢

## 4.2 Splatoon 系UIから学ぶもの

* 太い形
* 強いシルエット
* 色面の強さ
* 楽しい崩し
* 大胆な非対称性
* “遊び”のある情報提示

## 4.3 Zenless Zone Zero 系UIから学ぶもの

* UI自体のグラフィック性
* ステッカー / ラベル / 印刷物のような物質感
* ちょっとした工業製品感
* 大小差
* タイポグラフィと形状の一体感

## 4.4 Arknights 系UIから学ぶもの

* 強い情報階層
* 整理された構成
* 制御された非対称性
* Dense / Loose の使い分け
* 画面全体の秩序感

---

<br/>
<br/>

# 5. 参考にしないもの

以下の方向へは寄せない。

* 一般的なSaaSダッシュボードのテンプレそのまま
* 紫青グラデーション主体の“AIっぽいUI”
* 線だけのアイコン中心で軽すぎるUI
* 絵文字的な表現
* 安価なクリップアート風ファイルアイコン
* 汎用ゲームUI素材集そのままの見た目
* 全部が大きなカードで区切られたUI
* 全部が同じ角丸長方形で構成された無個性UI

---

<br/>
<br/>

# 6. 守っただけでは合格しない

この文書には禁止が多い。「〜しない」「避ける」「禁止」と書かれた指示は50項目を超える。
対して「こうしろ」の側は、**太い形・強いシルエット・少しコミカル**のような、測れない言葉が中心である。

この偏りをそのまま実行すると、次のことが起きる。

> **危ういものを全部削った結果、ただの暗い IDE が残る。**

実際に本文書を絵に起こして確かめた（`MyDocs/ローカルイシュー/UIデザインルールの作成/サンプル生成/`）。
禁止事項は**1つ残らず守られていた**うえで、Graphic・Playful・Premium・物質感が丸ごと欠落した。

## 6.1 この失敗の見分け方

次のどれかに当てはまったら、この失敗に落ちている。

* バッジとステッカーを全部剥がすと、汎用のダークテーマIDEと区別が付かない
* 白黒にすると、画面の構成に特徴が何も残らない
* 「らしさ」を挙げろと言われて、色の話しか出てこない
* 楽しさを担っている部品が1つか2つしかない

## 6.2 だから合格条件は2つある

**禁止事項に触れていないことは、合格条件の半分でしかない。**

| 側 | 何を決めるか | どこに書いてあるか |
|---|---|---|
| 天井 | やってはいけないこと | 禁止事項の節・参考にしないものの節・Compact List Mode の絶対ルール |
| 床 | 最低限やらなければいけないこと | **必達要素と最低数（Floor）の節** |

**床を持たない規則は、必ず最低品質へ収束する。** 迷ったら削る、を繰り返すと何も残らないためである。

## 6.3 「参考にする方向性」は参考ではない

**参考にする方向性の節は、要求である。**

節名が「参考」なので読み飛ばされやすいが、本プロジェクトのゲーム性は事実上あの節にしか書かれていない。
太い形・強いシルエット・色面の強さ・大胆な非対称性・印刷物の物質感・大小差・タイポグラフィと形状の一体感——
これらを渡さずに作ると、上の失敗にそのまま落ちる（実測）。

**実装時は、あの節を「守るべき仕様」として読むこと。**

---

<br/>
<br/>

# 7. 基本思想

## 7.1 情報設計を最優先する

見た目の楽しさは重要だが、優先順位は以下とする。

1. 分かりやすい
2. 操作しやすい
3. 探しやすい
4. 一貫している
5. 高品質
6. 楽しい
7. 派手

「派手さ」は最下位である。

### この順位は、下位を省略してよいという意味ではない

順位が決めているのは、**衝突したときにどちらを取るか**だけである。

上から順に満たしていく読み方をすると、6位と7位には決して到達しない。
実際にそれが起きて、楽しさが丸ごと欠落した（§6）。

したがって **§8 の床は、この順位と無関係に必達**とする。

---

## 7.2 UI脳と娯楽脳を両立する

新しい画面を作るときは、常に次の2つを意識する。

### UI脳

* 何をする画面か
* 何が重要か
* どこを押すか
* どう読むか
* 最短で操作できるか

### 娯楽脳

* 少し気持ちいいか
* 少し印象に残るか
* 少し触りたくなるか
* 無機質すぎないか
* このプロジェクトらしさがあるか

---

## 7.3 「コミカル」の定義

本プロジェクトでいうコミカルとは、子供っぽさやおもちゃっぽさではない。

目指すのは、

> **真面目な情報を、少し大胆に、少し崩し、少し楽しく見せること**

である。

例：

* タブが少し飛び出す
* 選択状態が少し持ち上がる
* ラベルがステッカー的に見える
* 状態表示に小さなユーモアがある

---

<br/>
<br/>

# 8. 必達要素と最低数（Floor）

守っただけでは合格しない節の「床」を、数で定める。

主要画面1枚には、以下を**最低数以上**入れる。上限は禁止事項の節と色の比率の節が決める。

| 必達要素 | 最低数 | 置く場所 | 満たしていない例 |
|---|---|---|---|
| Display Typography の見出し | 1 | 画面の主題を示す位置 | 既定のUIフォントを太字にしただけ |
| **面**で出す Primary Accent | 1 | 選択・現在地・主要操作のいずれか | アクセント色が細い線と小バッジにしか出ていない |
| Controlled Asymmetry の崩し | 2 | 別々の部品に。少なくとも1つは目に入る位置 | 画面の隅を1か所切っただけ |
| 物質感を持つ面 | 2 | 物質感の節の適用先の表から選ぶ。うち1つはステッカー以外 | ステッカーだけが物質を持っている |
| Dense な領域 / Loose な領域 | 各1 | 一覧はDense、見出し帯と操作群はLoose | 全領域が同じ行間・同じ密度 |
| 目に見える反応 | 3 | Hover / Selected / Pressed | Selected しか作っていない |

## 8.1 「最低1つ」を1つで済ませない

一画面一アクセントの節は**下限**であって、上限ではない。
「1つ入れたから終わり」と読むと、上の表の他の行が全部ゼロになる。

## 8.2 床は実サイズの画面で数える

部品を単体で見て数えない。**実際の画面に組んだ状態**で数える。
部品単体では入っているのに、画面に置くと他に埋もれて見えない、という形で落ちるためである。

## 8.3 天井を書いたら、床も書く

**この文書へ新しく規則を足すときの決まり。**

「使いすぎるな」「弱くしろ」「減らせ」だけを書くと、実装は**ゼロへ落とす**。
ゼロは常に安全で、常に規則違反にならないからである。

実際にこれを踏んだ。種別ごとの色に「弱くしろ」という上限だけを足したところ、
次の版で**色が丸ごと抜けて全部白一色**になった（§11.3）。§6 の失敗の縮小版である。

したがって、**抑制の規則を書いたら、同じ場所に下限を併記する。**

| 書いたもの | 一緒に書くもの |
|---|---|
| 〜しすぎるな | 最低これだけは要る |
| 弱くしろ | ここまでは弱くしてよい |
| 減らせ | ゼロにはするな |

## 8.4 数の規則は、守れる形で書く

**数を数えさせる規則は、現実の量と合っていないと無視される。**

ツリーに出る塗りフォルダを「7個まで」と書いたところ、そのまま無視された（§25.2）。
ツリーには本来フォルダが何十個も並ぶので、**上限そのものが守れない量だった**ためである。

数で縛る前に、**扱いで縛れないか**を考える。

* 悪い：塗りフォルダは7個まで
* 良い：塗ってよいのは開いている枝と現在地だけ。閉じた枝は彩度を落とす

**数は、規則そのものではなく、守れているかを確かめる症状として使う。**

---

## 8.5 優先順位は、省略の許可ではない

情報設計を最優先する節の順位（分かりやすい → … → 楽しい → 派手）は、
**衝突したときにどちらを取るか**を決めるものである。

**下位だから作らなくてよい、という意味ではない。**
上から順に満たしていくと下位に到達しないので、**床は順位と無関係に必達**とする。

---

<br/>
<br/>

# 9. 画面全体の構成方針

## 9.1 画面はグラフィックとして成立させる

画面を単に「部品の集合」として作らない。
主要画面では、一枚のグラフィックとして成立することを目指す。

ただし、ファイル一覧のような高密度エリアそのものは、規則性と一覧性を優先する。

---

## 9.2 Dense / Loose を作る

全領域を同じ密度で埋めない。

* 密な領域
* 疎な領域
* 強い領域
* 静かな領域

を意図的に作る。

---

## 9.3 一画面一アクセント

各主要画面には、最低1つは印象に残る特徴を持たせる。

例：

* 強い見出し
* 特徴的なタブ
* 非対称パネル
* 印象的な選択表現
* 大きな色面
* 目立つステータス表示

ただし、全部盛りにはしない。

### 「最低1つ」は下限であって、上限ではない

**ここを「1つ入れたら終わり」と読まないこと。**
アクセントの数を1つに絞る話であって、§8 の床の他の行までゼロにしてよいという意味ではない。

「全部盛りにしない」が効くのは、**床を満たしたあと**である。

---

<br/>
<br/>

# 10. Shape Language

## 10.1 基本形状

本プロジェクトでは、次のような形状体系を基本とする。

### Primary Panel

* Rectangle ベース
* 軽い角丸
* 必要に応じて一部 Cut Corner

### Secondary Panel

* よりシンプルな Rectangle

### Badge / Tag

* Capsule
* Sticker
* Tag

### Tab

* 飛び出し
* 切り欠き
* Slight Slant

### Accent Element

* Bar
* Dot
* Triangle
* Stripe
* Corner Mark
* Label

---

## 10.2 Controlled Asymmetry

完全な対称・完全な均一グリッドだけで構成しない。

使ってよい崩し：

* 一角だけ切る
* 一辺だけ伸ばす
* ラベルを少し重ねる
* 一部だけ色を変える
* 一部だけ少し傾ける

ただし、雑に崩すのではなく、**制御された非対称性**であること。

---

## 10.3 角丸

目安：

* Small: 4〜6px
* Standard: 8〜10px
* Panel: 10〜14px
* Large: 16px前後

何でも巨大な角丸にしない。

---

<br/>
<br/>

# 11. カラーシステム

## 11.1 ベースカラー

ダークニュートラルを基本とする。

例：

* Background: `#0B0F14`
* Surface: `#12161D`
* Elevated Surface: `#1A202A`
* Border: `#273041`
* Text Primary: `#E6EBF1`
* Text Secondary: `#9AA4B2`

完全な真っ黒一色は避ける。

---

## 11.2 アクセントカラーは役割で固定する

**候補を並べるだけでは決まらない。** 画面ごとに違う色が選ばれて統一が壊れる。
そこで**役割ごとに1色へ固定する**。

| 役割 | 色 | 使う場所 | 使わない場所 |
|---|---|---|---|
| Primary Accent | Cyan `#3DD9E6` 系 | 現在地・選択・主要操作・進行中 | 種別の区別 |
| Secondary Accent | Amber `#F5A623` 系 | Folder・注意・保留 | 主要操作 |
| Positive | Lime `#8FD14F` 系 | 完了・同期済み | 装飾 |
| Negative | Coral `#FF5A5F` 系 | エラー・破壊的操作 | 装飾 |

この4色は既定である。プロジェクトの都合で変えてよいが、
**変えるときは表ごと差し替える。候補を列挙しただけの状態へ戻さない。**

### Primary Accent は面で出す

比率（§11.3）の 10〜15% は、**細い線と小さなバッジだけでは到達しない**。
最低1か所は**面**で出す。

面の例：選択行の背景ティント／現在地の塗り／主要操作ボタンの塗り／見出し帯の下敷き

線と点だけで済ませると、画面はニュートラル一色に見える（実測で発生した）。

---

## 11.3 色の比率

目安：

* Neutral: 75〜85%
* Primary Accent: 10〜15%
* Secondary Accent: 5%前後

一画面に色を使いすぎない。

### 種別の色は第3層として扱う

§22 はファイル種別ごとに色の方向（Green系・Red系など）を挙げている。
これを**そのままの彩度で全種別に割り当てると、一画面の色数が8色を超えて上の比率が崩壊する**（実測で発生した）。

したがって種別色は、アクセントとは別の**第3層**として次の制約を課す。

* 種別色が占めてよいのは、**アイコンの中だけ**。行・背景・バッジへは広げない
* 彩度と明度を落とし、**Primary Accent より必ず弱く**する
* 一覧に同時に見えてよい種別色は **5色まで**。それを超える種別は近い色へ寄せる
* **種別色も比率の計算に入れる**（Neutral 側の残りを食う）

**種別を色で見分けさせない。** 色は補助であって、見分けるのは形の役目である（§18.4）。

### 「弱くする」は「消す」ではない

上は天井だけを並べた表である。**天井だけを課すと、色が丸ごと抜けて全部白一色になる**（実測で発生した）。
それは §6 の失敗の、色における現れ方である。

そこで床も置く。

* 一覧に同時に見えている種別色は、**3色以上**
* 主要な種別（画像・動画・音声・表・コード）は、**色を持つ**
* 色を落としたときに形だけで区別が付くこと（§18.4）は、
  **色を持たない理由にはならない。** 形は判別のため、色は走査のためにある

**形で判別でき、かつ色で走査できる**状態が正解であって、どちらか片方ではない。

---

## 11.4 Gradient

Gradientは禁止ではないが、主役にしない。

特に以下のような“AI SaaS感”の強いものを安易に使わない。

* Purple → Blue
* Blue → Pink
* 透明感だけに頼るグラデーション

---

<br/>
<br/>

# 12. 物質感

## 12.1 許容される物質感

以下のような軽い物質感は使ってよい。

* Printed Sticker
* Paper
* Label
* Matte Surface
* Plate
* Tape
* Painted Surface

---

## 12.2 ただし情報を邪魔しない

物質感は、**なくなっても意味が分かる**ことが前提。

特に小型アイコンでは、質感は主情報にしない。

---

## 12.3 どこに物質を持たせるか

§12.1 が決めているのは**素材**であって、**貼る場所**ではない。
貼る場所を決めないと、ステッカーだけが物質を持ち、他が全部フラットになる（実測で発生した）。

| 面 | 与える物質 | 強さ |
|---|---|---|
| 見出し帯・セクション見出し | 印刷面・プレート | 中 |
| タブ・現在地 | 印刷面・貼り紙 | 中 |
| 状態バッジ | ステッカー | 強 |
| 主要操作ボタン | マット塗装・プレート | 中 |
| パネルの縁 | 紙の厚み・段差 | 弱 |
| 一覧の行 | **持たせない** | — |
| ファイル名・サイズ・更新日時 | **持たせない** | — |

### 数の少ない面に置く

**一覧の行に物質を持たせない**のは、行が画面で最も数が多く、
質感が付くとその分がそのままノイズになるためである。

物質は、**画面に数個しか無い面**に置く。ここを守ると、少ない物量で物質感が立つ。

---

<br/>
<br/>

# 13. タイポグラフィ

## 13.1 基本方針

日本語・英数字ともに読みやすい Sans Serif を基本とする。

重要なのはフォント名より、

* サイズ
* ウェイト
* コントラスト
* 行間
* 字間

の統一である。

---

## 13.2 情報階層

目安：

* Page Title: 20〜28px / Semibold〜Bold
* Section Title: 15〜18px / Semibold
* Main Item: 13〜16px / Medium〜Semibold
* Secondary Info: 11〜13px / Regular
* Badge: 10〜12px / Medium〜Semibold

---

## 13.3 Functional Typography

以下は読みやすさ最優先。

* ファイル名
* サイズ
* 更新日時
* 説明文
* 設定項目
* 長文

---

## 13.4 Display Typography

以下には少し個性を出してよい。

* 見出し
* 短いラベル
* 状態表示
* 数値表示
* セクション名

ただし長文には使わない。

---

<br/>
<br/>

# 14. アイコン全体方針

## 14.1 最重要ルール

本プロジェクトでは、

> **Filled / Solid / Duotone 主体**

を基本とする。

Thin Outline だけの体系を主役にしない。

---

## 14.2 コンテンツアイコン

* 面積がある
* 太い
* シルエットが強い
* 小さくしても意味が残る
* 少しデフォルメされている

ことが理想。

---

## 14.3 操作用アイコン

以下のような操作アイコンは Outline でも可。

* Search
* Close
* More
* Filter
* Sort
* Expand
* Back

ただし全画面を細線だけにしない。

---

## 14.4 絵文字禁止

正式UIに OS Emoji を使わない。

例：

* 📁
* 📄
* 🖼️
* ⚙️
* 📝

---

## 14.5 クリップアート風禁止

以下を避ける。

* 安っぽい黄色フォルダ
* 山と太陽そのままの古い画像アイコン
* 地球儀
* Windows既定風の古い見た目
* 安価な3Dアイコン

---

<br/>
<br/>

# 15. 操作アイコンとツールバー

操作用アイコンの節は Outline を許しているが、**許しただけでは同じ角丸四角が等間隔で並ぶ**。
それは参考にしないものの節が禁じている「全部が同じ角丸長方形で構成された無個性UI」そのものである（実測で発生した）。

したがってツールバーには次を課す。

## 15.1 器を持たせるのは主要操作だけ

* 同じ形の器を等間隔で並べない
* **主要操作は1つだけ塗る**（Filled + Primary Accent）。これが「面で出す Primary Accent」の1か所を兼ねてよい
* 残りは器なしのアイコン、または静かな器にする

理想は、**押す場所が1つに決まっていて、残りが邪魔をしない**状態である。

## 15.2 数が増えたらまとめる

* 5個を超えるなら、オーバーフローメニューへ畳む
* 並べ切ろうとしない

## 15.3 区切りは線より間隔

* 細線1本で区切らず、**間隔で群を切る**
* 群は2〜3個までにする

## 15.4 太さを本文と揃える

操作アイコンの線の太さは、**同じ画面の本文の文字の太さと揃える**。
細すぎると画面が痩せ、Filled 主体の方針と食い違って見える。

---

<br/>
<br/>

# 16. ファイルアイコン設計の大原則

ここが最重要である。

本プロジェクトでは、ファイル一覧内のアイコンは**比較的小さく表示される**。

したがって、ファイルアイコンは

> **拡大して見たときの豪華さより、実際の小サイズで瞬時に判別できること**

を優先する。

---

<br/>
<br/>

# 17. Small-Size First

## 17.1 実サイズ前提で設計する

ファイルアイコンは、最初から小サイズ前提で考える。

想定サイズ：

* 16px
* 18px
* 20px
* 22px
* 24px

特に本プロジェクトでは、

> **18〜22px前後で成立すること**

を重視する。

---

## 17.2 最終評価は実サイズで行う

大型のアイコンサンプル画像は、雰囲気確認用にすぎない。

最終的な採否は、

> **実際の一覧サイズに縮小し、複数並べた状態**

で判断する。

---

<br/>
<br/>

# 18. アイコンの幾何（測れる規定）

Micro Detail を禁止する節は「細かいものを入れるな」と言うが、**禁止だけでは太くならない**。
コンテンツアイコンの節の「面積がある・太い・シルエットが強い」も、そのままでは測れない。

そこで太さを数で決める。

## 18.1 設計グリッド

**24×24 のグリッドで描き、18〜22px で表示する。**

## 18.2 測れる下限

24pxグリッド上の値。

| 項目 | 下限 | 理由 |
|---|---|---|
| 主モチーフの外接寸法 | グリッドの **70%以上**（約17px） | 小さく描くと縮小時に消える |
| 線・棒の太さ | **2px以上** | 1pxは20pxで消える |
| 隙間・抜き | **2px以上** | 潰れて1つの塊になる |
| 1アイコンの要素数 | **3つ以下** | 1アイコン1メッセージ |
| 繰り返し要素の数 | **一方向に3つまで**（格子なら 3×3） | ジッパーの歯・グリッドのセル等 |

繰り返しは**総数ではなく、一方向の数**で数える。
格子を「セル5個まで」と総数で縛ると 2×2 しか作れず、表のアイコンが成立しない。
3×3 なら20pxで1セットが約5px残るので潰れない（実測）。

### 太さの下限は、アイコンの型で変える

Filled 主体を数で担保したいが、**塗り面積の一律の下限は守れない**。
`<>` や `{}` や波形のように、**記号そのものが種別を表す型**は、構造的に面積を稼げないためである。
一律に「塗り面積40%以上」と書いたところ、`<>` が満たせず無視された（実測）。

そこで型ごとに分ける。**どちらか片方を満たせばよい。**

| 型 | 例 | 下限 |
|---|---|---|
| 面型（外形が意味を持つ） | Folder・画像・表・ジッパー | **塗り面積が外接矩形の 40%以上** |
| 記号型（記号が意味を持つ） | `<>`・`{}`・波形・再生三角 | **線の太さがグリッドの 1/8 以上**（24pxなら3px以上） |

記号型を面型の基準で測らないこと。**測れない基準は守られない**（§8.4）。

## 18.3 共通の器へ逃げない

Compact List Mode の絶対ルールの節は「紙型」を名指しで禁じているが、
**禁じているのは形の名前ではなく、共通の器に記号を入れて量産する作り方**である。

紙型を消しても、**角丸タイル・円・盾・バッジ**に置き換えれば同じ失敗になる（実測で発生した）。
外形が全種類で共通になると、20pxでは**色でしか区別できなくなる**。

したがって：

* 種別ごとに**外形そのものを変える**
* 器を使うのは、**外形で語れない一部の種別だけ**にする

### 割合では判定しない

かつてここに「器を使う種別が全体の半分を超えたら作り直す」と書いていたが、**これは効かない**。

10種のうち2種だけを器に入れても、**その2種が互いに衝突していれば不合格**である。
実際に画像と動画だけが角丸四角の器に入り、単色20pxで見分けが付かなくなった（実測）。
割合は合格していたのに、結果は失敗だった。

**判定するのは割合ではなく、§18.4 の結果である。**

---

## 18.4 判定：塗り一色にして並べる

**この節が、アイコン一式の合否を決める門である。** 他の節は、ここを通るための手引きにすぎない。

### 手順

1. 一式を**全部同じ1色の塗り**にする（色情報を完全に捨てる）
2. **実サイズ（18〜22px）**で横に並べる
3. 隣り合うものが互いに見分けられるかを見る

見分けが付かない組があれば、それは色に依存している証拠なので**作り直す**。

これはレビューチェックリストの「色だけに依存していない」を、**実際に確かめる手順**である。
目で「たぶん大丈夫」と判断しない。**色を落とす操作を実際に行う。**

### 衝突が出たときの解き方

衝突は必ず**2つ以上の種別の組**として出る。どちらを直すかを毎回悩まないよう、順番を決めておく。

1. **どちらかが器に入っているなら、器に入っている側を作り直す**（例：画像と動画が同じ角丸四角 → 両方を器から出す）
2. **どちらも器に入っていないなら、一覧に出る頻度が低いほうを作り直す**（例：文書と PDF → PDF 側を独立シルエットへ）
3. **どうしても差を付けられないなら、統合する**。同じ印を共有すると決めて書く（§18.5）

**やってはいけないのは、差を微細装飾で付けることである。** 折り目・小さな角の欠け・薄い柄で分けても、
20pxでは消えて衝突が残るうえ、§20.5 の Micro Detail 禁止にも触れる。

### 作り直しは、意味を削ることがある

衝突を解こうとして外形を変えると、**その種別が何なのか伝わらなくなる**ことがある。
実際に PDF を文書から引き剥がしたところ、衝突は解けたが、
出来上がった形からは PDF だと読み取れなくなった（実測）。

**見分けが付くが意味が分からない形は、失敗である。** そのときは 3（統合）を選ぶ。

* 見分けが付き、意味も分かる → 採用
* 見分けが付くが、意味が分からない → **統合する**（§18.5）
* 見分けが付かない → 作り直す

種別を1つ減らすことは負けではない。**判別できない印を10個持つより、判別できる印を8個持つほうがよい。**

## 18.5 同じ印になる種別は、同じ印だと決めておく

外形で差を付けられない種別（例：プレーンテキストと Markdown）は、
**同じ印を共有してよい。ただし共有すると決めて書く。**

曖昧にしたまま各自が「なんとなく違う印」を作ると、
差が微細装飾に逃げて、小サイズで区別できないものが増える。

---

<br/>
<br/>

# 19. ファイルアイコンの2モード

本プロジェクトのファイルアイコンは、用途に応じて2モードで考える。

## 19.1 Compact List Mode

一覧・ツリー・サイドバーなどで使う標準モード。

特徴：

* 小型
* 軽量
* 高判別性
* 文字ラベルなし
* 紙型なしが基本
* 微細装飾なし
* ファイル名を邪魔しない

**これが標準。**

---

## 19.2 Rich Detail Mode

以下のような大きめ表示で使う拡張モード。

* Empty State
* ファイル詳細画面
* 説明資料
* アイコンギャラリー
* チュートリアル
* ファイルタイプ選択
* 大型カード

こちらでは、

* 紙型
* ラベル
* ステッカー
* 質感
* 追加装飾

を使ってよい。

ただし、

> **Rich Detail Mode のアイコンをそのまま縮小して Compact に流用しない。**

---

<br/>
<br/>

# 20. Compact List Mode の絶対ルール

以下は推奨ではなく、**原則ルール**である。

## 20.1 文字情報を入れない

Compact List Mode では、アイコン内に以下を原則入れない。

* 拡張子
* 種別名
* 略称
* 解像度
* 小型タイポグラフィ

禁止例：

* IMG
* PPT
* PDF
* ZIP
* JS
* CODE
* XLSX
* 1024×768

理由：

* 小サイズでは読めない
* ファイル名側で重複している
* ノイズになる

---

## 20.2 読めない文字は装飾としても使わない

「雰囲気のため」の小文字も原則禁止。
読めない文字は、小サイズでは単なるノイズである。

---

## 20.3 共通の器に記号を入れる作り方を標準にしない

Compact List Mode では、

> **すべてのファイルを “共通の器 + 中央の記号” にする設計を標準にしない。**

つまり、

* 紙 + 音符
* 紙 + 写真
* 紙 + グリッド
* 紙 + `<>`

の量産を避ける。

理由：

* 小型化すると全部同じ外形に見える
* カテゴリ差が弱くなる

### 禁じているのは「紙」という形ではなく、「共通の器」という作り方

**紙型を消しただけでは解決しない。**
角丸タイル・円・盾・バッジへ置き換えると、同じ失敗がそのまま再現する（実測で発生した）。

外形が全種別で共通になると、20pxでは**色でしか区別できなくなる**。
これは §34.1 の「色だけに依存していない」に不合格である。

したがって：

* 種別ごとに**外形そのものを変える**
* 器を使うのは、**外形で語れない一部の種別だけ**にする
* 器を使う種別が全体の**半分を超えたら作り直す**

判定の手順は §18.4（塗り一色にして並べる）。

---

## 20.4 独立シルエットを第一選択にする

Compact List Mode では、

> **カテゴリ自体を象徴する独立した Filled シルエット**

を第一候補とする。

例：

* Folder → フォルダ形そのもの
* Image → 画像シンボルそのもの
* Video → Play / Film そのもの
* Audio → 音符 / 波形そのもの
* Spreadsheet → Grid そのもの
* Code → `<>` / `{}` そのもの
* Archive → Zipper そのもの

---

## 20.5 Micro Detail を禁止する

小サイズで意味を失う微細装飾を入れない。

禁止例：

* 小ドット
* 小ストライプ
* バーコード
* 微細ハーフトーン
* 小さな折り目
* 小ラベル
* 細かな柄
* 極小テクスチャ差
* 極小ステッカー

---

## 20.6 1アイコン1メッセージ

1つのアイコンに複数の説明要素を詰め込まない。

理想：

> **1アイコン = 1カテゴリ = 1主要モチーフ**

---

<br/>
<br/>

# 21. Folder の特別ルール

Folder は特に重要。
ユーザーのフィードバックを踏まえ、Folder には専用ルールを設ける。

## 21.1 Folder は一覧の基準点

Folder は一覧の中で頻出しやすく、情報構造上も重要な存在である。
そのため Folder は、

> **他のファイル種別より少し静かで、少しシンプルで、少し安定しているべき**

である。

---

## 21.2 Folder はガチャつかせない

Folder には以下を原則入れない。

* 中央の灰色パネル
* 中央の柄
* 中央の別色図形
* 大きな内部記号
* 文字ラベル
* ステッカー
* 過剰なストライプ
* 複数の装飾

特に、

> **フォルダ中央に灰色の柄や内部模様を置かない。**

---

## 21.3 Folder に許可される装飾

Folder に許される装飾は最小限。

許容例：

* タブ形状
* 一角だけの軽い Cut
* 1か所だけの小さな Accent
* ごく軽いストライプ
* 軽い物質感

ただし、**あっても1つ程度**でよい。

---

## 21.4 Folder の理想

Folder は、

* 一瞬で Folder と分かる
* 外形と色だけで成立する
* 小さくしても潰れない
* 他より少し静か
* しかし安っぽくはない

状態を理想とする。

つまり、

> **Folder は地味ではなく、静かに洗練されていることを目指す。**

---

<br/>
<br/>

# 22. ファイル種別ごとの方向性

以下は Compact List Mode 前提の推奨方針。

## 22.1 Folder

* タブ付きフォルダ形状
* Filled 主体
* Yellow / Amber 系
* 最も静か
* 最もシンプル
* 文字なし
* ステッカーなし
* 中央柄なし

---

## 22.2 Generic Document / Text / Markdown

* 紙型は必須ではない
* 文書感を示す大きな行・段落の抽象化は可
* `T` や `TXT` に頼らない
* 文書カテゴリは**独立シンボル化する**（「可能なら検討」ではない）

### この3つは1つの印を共有してよい

Text と Markdown と Generic Document は、**外形で差を付けきれない**。
無理に差を付けようとすると、差が微細装飾へ逃げて小サイズで消える。

したがって **3つで1つの印を共有すると決める**（§18.5）。
差を付けたくなったら、装飾ではなく**行の側の情報**（拡張子付きのファイル名）で見分けさせる。

---

## 22.3 Spreadsheet / CSV / XLSX

* 大きな Grid を主役にする
* Green 系
* 細かすぎるセルは禁止
* 紙型より独立グリッドを優先
* `XLS` `CSV` `XLSX` などの文字ラベルは禁止

---

## 22.4 Presentation

* 円グラフやプレゼン構成を示す太いモチーフ
* Orange / Coral 系
* `PPT` ラベルは禁止
* 紙型より独立したプレゼン記号を優先

---

## 22.5 Image

* 風景 / 写真 / フレーム感を示す大きな形
* `IMG` ラベル禁止
* 解像度表記禁止
* 山 + 円などの大形状を優先
* 紙型より独立画像シンボルを優先

---

## 22.6 Video

* Play Triangle / Film など
* Magenta / Red / Dark 系
* 小さなフィルム穴の量産は禁止
* `VIDEO` `MP4` などの文字禁止

---

## 22.7 Audio

* 太い音符 / 波形
* Teal / Purple 系
* `MP3` などの文字禁止
* シルエット重視

---

## 22.8 Code

* `<>`
* `{}`
* `/`

などの太い主要記号を使う。

* `CODE` ラベル禁止
* `JS` `TS` `HTML` `CSS` などの文字禁止
* 紙型より独立記号を優先

---

## 22.9 Archive

* Zipper を主役にする
* `ZIP` ラベル禁止
* テープ風ラベル禁止
* できるだけ外形か主モチーフでカテゴリ感を出す

---

## 22.10 PDF

* PDFらしい重みや文書感は出してよい
* ただし `PDF` ラベルは禁止
* Red 系は使ってよい
* Compact では極力単純化する

---

<br/>
<br/>

# 23. ステッカー表現の再定義

ステッカー感は本プロジェクトの重要な魅力である。
ただし使いどころを整理する。

## 23.1 種別情報には使わない

Compact Icon では、

* PDF
* IMG
* ZIP
* PPT
* JS

などの**種別情報をステッカー化しない**。

---

## 23.2 状態情報に使う

ステッカーは主に以下に使う。

* NEW
* ERROR
* SYNC
* SHARED
* LOCKED
* FAVORITE
* LOCAL
* BETA

つまり、

> **種別は形で示し、状態はステッカーで示す。**

---

## 23.3 ステッカーは例外だから効く

**用途を決めても、数を決めないと列になる。**
全行に状態バッジが付くと、ステッカーは表の1カラムへ退化し、
貼ってある感じも、目を引く力も消える（実測で発生した）。

したがって数を決める。

* 一覧で同時に見えるステッカーは、**1つ以上、可視行の3割まで**
* **縦に連続させない**。並ぶと列に見える
* 位置を**揃えない**。揃えるとカラムになる
* **専用の列を作らない**。行の中で名前や余白に軽く重ねる

上限だけでなく**下限も置いてある**。ゼロにすると、この文書の魅力の1つが画面から消えるためである（§8.4）。

### 全行に出したい情報は、ステッカーではない

全行に出る必要がある情報（同期状態など）は、状態ではなく**属性**である。
属性はステッカーではなく、**行の静かな要素**（小さな点・薄い文字・行末の印）で示す。

ステッカーへ回すのは、**目を引くべき少数**だけにする。

---

<br/>
<br/>

# 24. ファイル一覧UIの基本方針

## 24.1 一覧構造

ファイル一覧は、

> **VS Code / Google Drive のような規則正しい縦リスト**

を基本とする。

例：

```text
[ICON] filename.ext                 [status] […]
       metadata
```

---

## 24.2 1行ごとに巨大カード化しない

一覧の各行を大型カードにしない。
高密度でスキャンできることを優先する。

---

## 24.3 行の高さ

目安：

### Desktop Compact

* 36〜40px

### Desktop Standard

* 44〜52px

### Mobile / Touch

* 48〜60px

---

## 24.4 ファイル名を主役にする

一覧での視覚優先順位：

1. ファイル名
2. アイコン
3. 状態
4. メタ情報
5. アクション

アイコンがファイル名より強くなりすぎない。

---

## 24.5 拡張子はファイル名側で見せる

例：

* `account.spec.ts`
* `README.md`
* `image.jpg`
* `data.csv`

これにより、アイコン側で種別文字を繰り返す必要をなくす。

---

<br/>
<br/>

# 25. サイドバー・ツリー

サイドバーとツリーはこの文書の対象に挙がっているが、規定が無いと
**同じフォルダアイコンが縦に並ぶだけの帯**になる（実測で2回とも発生した）。

塗りのフォルダを10個以上縦に並べると、**そこが画面で最も強い色面**になり、
主役であるはずのファイル名より目立ってしまう。

## 25.1 深さは色ではなく、位置と線で示す

* 階層はインデントと接続線で示す
* **深さごとに色を変えない**
* 開いている枝と閉じている枝は、三角の向きだけでなく**明るさでも差を付ける**

## 25.2 数ではなく、扱いで抑える

ツリーには本来フォルダが何十個も並ぶ。**数を上限で縛る規則は現実に合わず、守られない**（実測で無視された）。
縛るのは数ではなく、**枝の状態ごとの扱い**である。

| 枝の状態 | 扱い |
|---|---|
| 現在地 | Primary Accent の**面** |
| 開いている枝 | 塗りの Secondary Accent（＝通常のフォルダ） |
| 閉じている枝 | **彩度を落とす**か、輪郭だけにするか、印を出さない |

**塗りの色面を持ってよいのは、開いている枝と現在地だけ**である。
閉じた枝まで同じ強さで塗ると、ツリー全体が画面で最も強い色面になり、
主役であるファイル名を食う（実測で2回発生した）。

### 症状での確認

規則を守れているかは、数で確かめる。

* 同じ強さの塗りフォルダが**縦に7個以上**続いていたら、扱いを分けられていない

## 25.3 現在地は面で示す

現在いる枝は Primary Accent の**面**で示す。
これは必達要素の「面で出す Primary Accent」の1か所を兼ねてよい。

## 25.4 幅

* 目安 18〜26%
* ファイル名が主役なので、**サイドバーが主役を食わない**こと

---

<br/>
<br/>

# 26. 一覧で遊ぶ場所 / 遊びすぎない場所

## 26.1 遊ぶ場所

* アイコン
* ヘッダー
* バッジ
* 選択状態
* Hover
* Dragging
* Empty State
* フィルター
* タブ

## 26.2 遊びすぎない場所

* ファイル名
* 更新日時
* サイズ
* 長文説明
* 高密度一覧の本文

---

<br/>
<br/>

# 27. 状態とインタラクション

## 27.1 Default

通常状態は比較的静かに保つ。
常時 Glow させない。

---

## 27.2 Hover

候補：

* Surface が少し明るくなる
* Action が見える
* Accent Bar が現れる
* アイコンがごく軽く反応する

---

## 27.3 Selected

Selected は明確にする。

候補：

* 背景 Tint
* 左側の Accent
* Slight Lift
* ごく軽い Glow
* 状態バッジ

単なる 1px Border だけで済ませない。

---

## 27.4 Pressed

押下時には軽い反応を入れてよい。

例：

* `scale(0.97〜0.99)`

---

## 27.5 Dragging

ドラッグ中は「持っている感」を少し出してよい。

候補：

* 1.02倍程度
* 1〜2°の傾き
* Shadow 増加
* Drop Target 反応

---

## 27.6 Focus

キーボード Focus は明確にする。
ただしデフォルト青枠そのままではなく、テーマに統合する。

---

<br/>
<br/>

# 28. Motion

## 28.1 方針

Motion は見せるためではなく、**操作の意味を補強するため**に使う。

使ってよいもの：

* Fade
* Short Slide
* Small Scale
* Tiny Overshoot
* Highlight
* Small Glow
* Mask Reveal

避けるもの：

* 長い待ちアニメ
* 大きく跳ねる動き
* 毎回回転する動き
* 無意味な Particle
* 過剰な Elastic

---

## 28.2 Duration

目安：

* Micro: 80〜140ms
* Normal: 140〜220ms
* Panel: 180〜300ms
* Special: 300〜450ms

通常操作で 500ms 以上待たせない。

---

<br/>
<br/>

# 29. Empty State / Error / Loading

## 29.1 Empty State

単に「ファイルがありません」で終わらせない。

使ってよい要素：

* Filled 系アイコン
* 短い説明
* 行動を促すボタン
* 軽いユーモア

---

## 29.2 Error State

世界観に統合してよいが、意味は明確にする。

---

## 29.3 Loading

Skeleton Loading を使用してよい。
この Skeleton は Placeholder の意味である。

---

<br/>
<br/>

# 30. Design Token 化方針

以下は可能な限り Token 化する。

* Color
* Spacing
* Radius
* Shape
* Typography
* Icon Size
* Shadow
* Motion
* Z Index
* Badge Style
* Panel Style

画面ごとに毎回値を発明しない。

---

<br/>
<br/>

# 31. Coding Agent の実装手順

新規 UI を作る場合：

1. 画面の目的確認
2. 情報優先順位決定
3. 本ルール確認
4. 既存 Token / Component 確認
5. UI脳で最小構成を作る
6. **§4（参考にする方向性）を仕様として読み直す**
7. **§8 の床の表を上から埋める**
8. 過剰な装飾を削る
9. 実装
10. 実サイズでレビュー
11. **§34.4 で床を数え直す**（削る工程で床を割ることがある）

## 31.1 8と11の両方が要る

8（削る）だけを回すと §6 の失敗へ落ち、7（足す）だけを回すと装飾過多になる。
**足してから削り、削ったあとにもう一度数える。**

削る工程で床を割るのは実際に起きるので、11 を省かないこと。

---

<br/>
<br/>

# 32. ファイルアイコン作成手順

ファイルアイコンを作る場合：

1. Compact か Rich かを判定する
2. 実際の表示サイズを確認する
3. 20px前後で残るシルエットを決める
4. 大きな主モチーフを決める
5. 文字なしで成立するか確認する
6. **共通の器へ逃げていないか確認する**（紙型に限らない。§20.3）
7. 微細装飾を削る
8. **§18.2 の下限を測る**（外接寸法・塗り面積・線の太さ・隙間・要素数）
9. **塗り一色にして実サイズで並べ、見分けが付くか確かめる**（§18.4）
10. 一覧に複数並べて確認する
11. Folder が静かか確認する
12. 必要なら Rich 版を別設計する

## 32.1 9 を目で済ませない

「たぶん見分けが付く」で通すと、色に依存したまま出荷される。
**色を落とす操作を実際に行う**こと。手順であって、心構えではない。

---

<br/>
<br/>

# 33. 禁止事項

以下は原則禁止。

**ただし、この節を全部守っても合格ではない**（§6）。天井であって、床ではない。

* 絵文字を正式UIに使う
* Thin Outline だけを全面採用する
* すべてのファイルアイコンを共通の器（紙型・角丸タイル・円・盾）に入れる
* 種別を色だけで見分けさせる
* 状態ステッカーを全行に付けて列にする
* 塗りのフォルダアイコンを縦に並べ続ける
* ツールバーを同じ形の器の等間隔の列にする
* Compact アイコンに文字を入れる
* Compact アイコンに読めない補助情報を入れる
* 解像度などの無意味な情報をアイコンに入れる
* Folder を他種別と同じテンションで装飾する
* Folder の中央に灰色柄や内部模様を入れる
* Rich アイコンをそのまま縮小する
* すべてをカード化する
* すべてを大きな角丸長方形で構成する
* 紫青グラデーション頼みのAIっぽいUIにする
* 無意味なGlowを乱用する
* 無意味な装飾をばらまく
* 情報より装飾が目立つ状態にする

---

<br/>
<br/>

# 34. レビューチェックリスト

## 34.1 小型アイコンレビュー

16〜24pxで以下を確認する。

* [ ] 大まかなカテゴリが分かる
* [ ] シルエット差がある
* [ ] 文字に依存していない
* [ ] 細かな装飾に依存していない
* [ ] **塗り一色にして並べても、衝突する組が無い**（§18.4 を実際に行ったか）
* [ ] ファイル名より強すぎない
* [ ] 20個並んでも騒がしくない
* [ ] Filled 主体を維持している
* [ ] エンタメ感を失っていない

**「色だけに依存していない」は、目で見て答える項目ではない。**
§18.4 の手順を実際に通した結果として答える。通していないなら、この項目は未チェックである。

衝突しやすい組は決まっているので、そこは特に見る。

| 衝突しやすい組 | なぜ |
|---|---|
| 画像 と 動画 | どちらも「四角の中に何か」に落ちやすい |
| 文書 と PDF | どちらも「紙＋何か」に落ちやすい |
| 表 と 一部の柄物 | 格子が他の繰り返し模様と混ざる |

---

## 34.2 Folder 専用レビュー

* [ ] 一目でフォルダと分かる
* [ ] 中央に不要な柄や灰色面がない
* [ ] 文字が入っていない
* [ ] ステッカーが付いていない
* [ ] 他のファイルより少し静かに見える
* [ ] 20pxで潰れない
* [ ] 一覧のアンカーとして機能する

---

## 34.3 全体レビュー

* [ ] VS Code / Google Drive 級に迷わず使えるか
* [ ] 一般的なSaaSダッシュボードより楽しいか
* [ ] 高品質なゲーム内に出ても安っぽく見えないか
* [ ] 情報より装飾が強くなっていないか
* [ ] 長時間見ても疲れにくいか

---

## 34.4 床のレビュー

§8 の表を、**実サイズの画面で数える**。

* [ ] Display Typography の見出しが1つ以上ある
* [ ] Primary Accent を**面**で出している場所が1つ以上ある
* [ ] 崩しが2か所以上、別々の部品にある
* [ ] 物質を持つ面が2つ以上あり、うち1つはステッカー以外である
* [ ] Dense な領域と Loose な領域が両方ある
* [ ] Hover / Selected / Pressed が3つとも作られている

---

## 34.5 「暗いIDE化」判定

§6 の失敗に落ちていないかを確かめる。**1つでも当てはまったら作り直す。**

* [ ] バッジとステッカーを全部外すと、汎用のダークテーマIDEと区別が付かない
* [ ] 白黒にすると、画面の構成に特徴が何も残らない
* [ ] 楽しさを担っている部品が2つ以下しかない
* [ ] サイドバーに同じ強さの塗りアイコンが7個以上縦に並んでいる
* [ ] ツールバーが同じ形の器の等間隔の列になっている
* [ ] 状態バッジが専用の列になっている
* [ ] ステッカーが1つも出ていない
* [ ] アイコンから種別色が抜けて、ほぼ単色になっている

**後ろの2つは、抑制のしすぎで落ちた形である**（§8.3）。
「やりすぎ」だけでなく「やらなさすぎ」でも、この判定に引っかかる。

---

<br/>
<br/>

# 35. 既存実装がこの文書と食い違うとき

本文書は実装より後に書かれたので、**既存のコードには本文書に違反したものが残っている**。

例：ファイル一覧の印を返している箇所が OS 絵文字をそのまま使っており、
絵文字禁止の節に正面から違反している（`web/src/lib/fileKind.ts`）。

## 35.1 扱い

* **見つけたら、まず違反として記録する。** その場で直さなくてよい。
  印の差し替えは、アイコン一式が揃ってからでよい
* ただし**新しく書くコードで違反を増やさない**。
  既存に合わせて絵文字を1つ足す、といった追随をしない
* 違反を残すと決めたときは、**なぜ残すのかをコード側か本文書に書く**

## 35.2 なぜ理由を書かせるのか

理由の無い違反が増えると、この文書は「守らなくてよいもの」になる。
**守られない規則は、無い規則より悪い。** 読む時間だけを取るためである。

---

<br/>
<br/>

# 36. 最終原則

> **使いやすさのために個性を消さない。**

> **個性のために使いやすさを壊さない。**

> **ゲームらしさは装飾の量ではなく、形・色・構図・反応・リズムで作る。**

> **Compact Icon では、種別は文字ではなく形で語る。**

> **Compact Icon では、すべてを紙型に押し込めない。**

> **Folder は最も静かで、最も安定した存在にする。**

> **Folder に中央柄や過剰装飾を入れない。**

> **他ファイルは Folder より少し遊んでよいが、小サイズで消える情報は入れない。**

> **ステッカーは種別ではなく状態に使う。**

> **高品質さは情報量ではなく、整理・強いシルエット・適切な抑制によって作る。**

---

<br/>
<br/>

# 37. Design Keywords

迷ったときは以下へ立ち返る。

* GRAPHIC
* PLAYFUL
* BOLD
* ASYMMETRIC
* TACTILE
* FILLED
* SMALL-SIZE FIRST
* RHYTHMIC
* PREMIUM
* CLEAR
* FUN

---

<br/>
<br/>

# 38. Final Design Statement

> **A practical tool that behaves like software, but feels like entertainment.**
> **Small enough to stay clear. Bold enough to stay memorable.**

実用ソフトとして明快であり、
小さく表示しても意味が伝わり、
それでも触って少し楽しいUIを作る。
