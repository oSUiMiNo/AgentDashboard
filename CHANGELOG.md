# 変更履歴

`dist` はこのファイルの**版の見出し**を読んで、リリースの本文に載せる。
**利用者が読む場所なので、実装の言葉ではなく「触って何が変わるか」を書く。**

とくに**戻れなくなる変更**は必ず書く。版の切替は「戻る先の版が、いま適用済みの
スキーマを知っているか」で判定するので、**記録の形が変わると画面から戻れなくなる**
——黙って配ると、利用者は理由の分からない拒否を見ることになる。

## 0.1.78

### 変えたところ
- **枠とカードは、掴み手ではなく本体をつまんで並べ替えるようになりました。** 余白でも
  カードそのものでも、押して動かせばそのまま運べます（**PJT 専用画面の横並びは
  これまでどおり左上の掴み手から**）
- **スマホは長押ししてから運びます。** ホーム画面でアプリを並べ替えるのと同じ形です。
  **そのままなぞれば、これまでどおり縦にスクロール**します
- **並べ替えが滑らかになりました。** 持っているものだけでなく、**押しのけられる側も
  滑って動きます**。動きが気になる場合は、設定の「静けさ」を「静止」にすると止まります
- **選んだ枠とカードが、地の色で分かるようになりました。** これまでは小さな白い点が
  付くだけで、しかも**復旧ボタンが出ているカードでは点がボタンの下に隠れて見えて
  いませんでした**。点はやめて、面ごと色が変わります

- **セッションに自分で名前を付けられるようになりました。** カードにマウスを乗せると
  左上に鉛筆が出ます（**スマホは長押しでカードを選ぶと出ます**）。押してその場で書き、
  Enter で決まります。空にすると消えます
- **付けた名前は、`/rewind` などで別のセッションへ乗り換えても付いてきます。**
  名前はカードではなく**会話そのもの**に付くので、同じ会話を別のカードで開いても
  同じ名前が出ます。**Claude が付ける名前は書き換わりません**（自分で付けていない
  カードは、これまでどおりそちらが薄く出ます）
- **一度外したセッションを、あとから呼び戻せるようになりました。** 枠の「＋」を押すと
  「どれを」の欄が出て、その PJT で過去に使ったセッションを選べます。**名前を付けた
  ものは消えません**——付けていないものは最近のぶんだけが並びます
- **もう履歴が残っていないセッションは、選択肢から消えます。** ただし**その PC が
  繋がっていないときは消しません**——見えないだけかもしれないので、「（未確認）」と
  付けたうえで選べるようにしてあります

### 分かっていること（まだ直っていません）
- **走っているセッションが「入力待ち」と表示されることがあります。** 端末は動いて
  いるのにカードだけが戻ってしまう状態で、**原因は追跡中**です。**指示を送れば直ります**
  （次のフックでカードが作業中へ戻ります）。倒れた瞬間の記録が残っていなかったので、
  この版から**追える材料をログに残す**ようにしました

### 直したところ
- **スマホでカードを長押しすると、カードではなく PJT の枠が選ばれていたのを直しました**
- **掴んだカードが傾いていなかったのを直しました。** 傾ける指定は書かれていたのですが、
  画面には一度も出ていませんでした

## 0.1.77

### 直したところ
- **スマホから画像を添付して送ると「Failed to fetch」になっていたのを直しました。**
  2枚以上付けたときや、付けてから送るまでに時間が空いたときに起きていました。
  付けた時点で画像の中身を控えるようにしたので、元の画像が入れ替わっても送れます
- **読めない画像を付けたとき、送信を押すまで分からなかったのを直しました。**
  付けた時点で「読めませんでした。もう一度選び直してください」と出ます
- **通信が届かなかったときに、英語のまま「Failed to fetch」と出ていたのを直しました**
- **カードが停滞のまま戻らなくなることがあったのを直しました。** ツール結果の続き行を
  「走っている印」と読み違えていました
- **版を入れ替えたあと、終わったセッションのプロセスが引き取られずに溜まっていたのを
  直しました。** 実機で 78 体溜まっていました

### 変えたところ
- **サイドバーで掘っていたフォルダと、開いていたファイルを覚えるようにしました。**
  読み込み直しても同じ場所へ戻ります
- **セッションに自分で名前を付けられるようにしました。**
  `agentdashboard session nickname <ID> <名前>`（`--clear` で消す）
- **引き取れていない子プロセスの数を、ログ・CLI・画面に出すようにしました。**
  3日間どこにも出ずに溜まっていたのが、今回の発端でした

### 分かったこと
- **スマホのキーボード上部にあるクリップボードの面からは、画像を貼り付けられません。**
  Chrome 側の制限で、こちらでは直せません。**入力欄を長押しして出る「貼り付け」**を
  使ってください（「＋」からの選択も使えます）

## 0.1.76

### 直したところ
- **起こした直後のカードが、一瞬だけ一覧から消えていたのを直しました。** 見た目には
  ほとんど分かりませんが、その一瞬に触ろうとすると空振りしていました

## 0.1.75

### 変えたところ
- **枠とカードを掴んで並べ替えられるようになりました。** 一覧の枠・一覧のカード・
  PJT 専用画面の区画のどこから並べ替えても、**動くのは同じ並び**なので、
  もう片方にもそのまま出ます
- **押し方が変わりました。** PC は**シングルで選ぶ・ダブルで開く**、
  スマホなど指で触る画面は**シングルで開く・長押しで選ぶ**（1枚選んだあとは
  シングルが「選ぶ」に変わります）。並べ替えようとして画面が変わってしまうのを
  防ぐためです。選択から抜けるには**一覧の地を押す**か **Esc**
- **選んだものを、まとめて起こす・外せるようになりました。** 1枚選んだ時点で
  上に帯が出ます。**電源は止まっているカードだけを起こします**——走っている
  claude を押し間違いで止めないためで、何枚が対象で何枚を飛ばすかは押す前に出ます
- **添付が札の形になりました。** ファイル名が出て、押すと大きく見られます
- **停滞したカードが、自分で入力待ちへ戻るようになりました。** これまでは
  ターンが完了せずに終わったとき（レート制限で止まった場合を含む）、
  端末は打てば動くのに表示だけ「停滞」のまま戻りませんでした

### 直したところ
- **スマホや LAN の IP から開いたとき、画像を選んでも貼っても何も起きなかったのを
  直しました**（素の HTTP で別ホストを開くと、ブラウザの機能が一部使えないため）
- **並べ替えた結果が画面で元へ戻ってしまうのを直しました**
- **選択の帯が出ると、カードを開けなくなるのを直しました**

## 0.1.74

### 変えたところ
- **ターミナルの切替に、オフのときも端末の印が出るようになりました。** これまでは
  入っているときだけ出ていたので、切れていると何の切替か分かりませんでした
- **枠とカードに並び順の列が入りました**

### 注意
- **この版より前へは、画面からは戻れなくなります**（記録の形が変わるため）。
  手で戻す場合は `<state_dir>/version-current` を消してください

## 0.1.73

### 変えたところ
- **セッションの操作（電源・終了・ターミナル・拡大／縮小）が、画面の帯から
  そのセッションの区画の真上へ移りました。** PJT 専用画面とセッション専用画面で
  **同じ操作が同じ場所**にあります
- **画面の行き来が1つのボタン（拡大／縮小）にまとまりました**
- **上の余白を詰めました**
