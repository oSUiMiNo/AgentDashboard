# 変更履歴

`dist` はこのファイルの**版の見出し**を読んで、リリースの本文に載せる。
**利用者が読む場所なので、実装の言葉ではなく「触って何が変わるか」を書く。**

とくに**戻れなくなる変更**は必ず書く。版の切替は「戻る先の版が、いま適用済みの
スキーマを知っているか」で判定するので、**記録の形が変わると画面から戻れなくなる**
——黙って配ると、利用者は理由の分からない拒否を見ることになる。

## 0.1.137
- **入力待ちのまま2時間が過ぎたセッションが、自動でスリープするようになった。** 入力待ちのカードも claude を1本抱え続けるので、放っておくと機械が重くなる——**30枚前後を抱えたまま1枚ずつ押して回るのは現実的でなかった**
- **寝たカードは一覧に残り、これまでどおり起こし直せる。** 人が「スリープ」を押したときとまったく同じ形になる
- **寝かせるのは「作業が終わって入力待ち」のものだけ。** **権限確認で止まっているものは寝かさない**（人の答えを待っているので、寝かせると問いごと消える）。サブエージェントが走っているものも、作業が終わっていないので寝かさない
- **端末に何か打っている最中は寝ない。** 送っていない打ちかけの指示は端末の中にしか無いので、消えると戻らない
- **一度に落ちるのは1秒あたり2枚まで。** しきい値を越えたカードが何枚溜まっていても、一斉には落ちない
- **時間は設定で変えられる**（`auto_sleep_idle_secs`、既定 7200 秒＝2時間）。**`0` にすると機能ごと止まる**

## 0.1.136
**枝分かれしたセッションで、構造化ビューとターミナルの中身が揃う版。**

- **枝分かれしたセッションの構造化ビューに、枝分かれ前のやりとりが出なくなった。** これまではターミナルには出ていないものが構造化ビューにだけ並び、**同じカードの同じ瞬間に2つの画面が別の量の会話を見せていた**
- **既に開いているカードは、そのままでは変わらない。** 直るのはこれから読むぶんで、**既に記録へ積まれた枝分かれ前のやりとりは残る**。呼び戻して新しいカードで開き直すと、伏せた形で出る

## 0.1.135
**呼び戻せなくなったセッションを、もう作らない版。**

- **⚠ この版から前の版へは戻れない。** 記録の形が変わっている（呼び戻し先を覚える欄が増えた）
- **寝かせたセッションが、二度と呼び戻せなくなることがなくなった。** これまでは、起こし直したときに claude が別の会話として名乗ることがあり、**そうなると元の会話が「＋」の一覧から消えて戻せなくなっていた**。記録の1割がこの形で壊れていた
- **復旧ボタンでも同じことが起きていたのを直した。** こちらのほうが重く、**押した結果、復旧しようとしていた会話そのものが一覧から消える**ことがあった
- **既に見失っていた会話のうち、枝分かれの親だったものは一覧へ戻る。** それ以外は戻せない——何を呼び戻そうとしたかは、起こした時点にしか残っていない情報なので、後から埋め直せない
- **呼び戻し一覧の上限（名前を付けていないもの20件）を外した。** これまでは21本目から黙って出なくなっていたので、**見失ったのと区別が付かなかった**
- **代わりに、名前を付けたものが先に並び、PJT ごとに畳まれるようになった。** 件数で捨てるのをやめたので、探し方のほうを用意した
- **履歴が消えているものも一覧に出るようになった。** これまでは黙って消えていたので探し続けることになっていた。**選べない形で「（履歴が消えています）」と出る**——押せてしまうと、静かに終わったカードが1枚増えるだけになるため
- **「＋」を開くたびに一覧を引き直すようになった。** これまではタブごとに1回しか引かなかったので、**一度開いたあとに寝かせたものは、画面を読み込み直すまで出てこなかった**
- **一覧を読めなかったときに「1本も無い」と言わなくなった。** サーバが落ちていても「過去のセッションはありません」と同じ見た目になっていた

## 0.1.134
- **タブを掴んで並べ替えるときの見た目が崩れなくなった。** これまでは掴んだ1枚の角が平らに切り落とされ、**傾いているようにも見えない四角**になっていた。掴むと浮いて見える
- **並べ替えたあとにタブの帯が勝手にずれることがあったのを直した。** 離した直後の、まだ滑っている位置を見て送っていた

## 0.1.133
- **画像も拡大縮小できるようになった。** これまでは「＋」「−」を押しても何も変わらなかった
- **既定は「入れ物に収める」**（大きい画像がそのまま原寸で出ると、何が写っているか分からないため）。**拡大すると器からはみ出すので、そのまま遡って見たいところへ行ける**
- **画像の下の「原寸で見る」で、基が原寸に変わる。** 大きな画像は 200% でも原寸に届かないので、**1:1 で細かいところを見たいときはこちら**（もう一度押すと収まる）。**遡っても付いてくる**ので、拡大しても押し戻せる

## 0.1.132
- **プレビュー（HTML）を、見ている姿のまま探せるようになった。** これまでは検索を押すと生テキストに切り替わっていたが、**整形された表示のまま印が付き、送るとその場が動く**
- **SVG だけは切り替えが要る。** あちらには同じ仕掛けを入れられないので、**「切り替えて探しますか」と先に聞く**（大きいときは、かかる時間も一緒に出る）。押さなければ何も変わらない
- **タブを掴んで運ぶと、押しのけられる側が滑って動くようになった。** これまでは瞬間で入れ替わり、何が起きたのか目で追えなかった（一覧のカードと同じ動きを使っている）
- **離したときにタブが飛ばなくなった**

## 0.1.131
**`0.1.128` で入れたファイルビュアの5つを、実際に触って出た穴ごと直した版。**

- **タブが、押していないのに並べ替わることがなくなった。** タブを押してすぐ帯の外へ指を抜くと掴んだままになり、**そのあと帯の上を通っただけで並びが変わって**いた
- **タブを運んでいる最中のカクつきを取った。** 動かすたびに記録を書き直していたのをやめ、**離したときだけ**覚えるようにした
- **速く運んでもタブが2つ飛ばなくなった**
- **運んでいる最中に帯が勝手に横へ流れて、狙った位置に落とせないことがなくなった**
- **タブを運んだ直後に、キーボードで選ぼうとした1回目が無視されることがなくなった**
- **Ctrl+Shift+← → が、焦点のあるタブを動かすようになった**（これまでは選ばれているタブを動かしていた）
- **タブを掴んで運ぶとき、名前が選択されて青くならなくなった**
- **タブの入れ替わる位置が、見た目どおりになった**（✕ のぶんだけ左へずれていた）
- **大きいプレビューで Ctrl+F を押しても、いきなり重い表示にならなくなった。** 時間がかかることを先に断るので、押すかどうかは決められる

## 0.1.130
- **サイドバーのファイルを長押し（PC は右クリック）すると、「ブラウザの新しいタブで開く」が出るようになった。** **スマホではこれが唯一の道である**——中クリックも Ctrl＋クリックもスマホには無く、右クリックの既定メニューはこのメニューが置き換えているので、ここに項目が無いと**ブラウザの新しいタブへ出る道が1本も残らなかった**
- **開く先は中クリックと同じ。** 道によって結果が違うことはない
- **フォルダには出ない**（行き先の URL が無いため）

## 0.1.129
- **サイドバーのファイル一覧が、リロードしなくても新しくなった。** claude がファイルを作ったり名前を変えたり消したりしても、これまでは**タブを読み込み直すまで開いたときのまま**だった。**10秒ほどで自動的に追いつく**——しかも一覧が「読み込み中…」へ落ちて点滅したりはしない
- **一時的に読めなくても、いま見えている一覧は消えない。** PC が寝ていても並びはそのまま残り、**赤い断り文が10秒おきに出たり消えたりすることもない**（自動の引き直しは黙って諦める）
- **裏に回したタブでは問い合わせない。** 表へ戻った瞬間に新しくなる。サイドバーを畳んでいる間も止まる
- **掘っていた場所と、押そうとしている行は動かない。** 中身が変わっていなければ並びに触れないので、コピーのボタンへ指を伸ばしている最中に的が逃げることはない

## 0.1.128
**ここに並ぶ5つは、コードが `0.1.127` のコミットへ紛れ込んでいる。** 共有している作業ツリーで**コミットが混ざり**、実装だけが1つ前の版へ入ってしまった。この版で足したのは説明だけである。

**【2026-09-09 訂正】この節はもともと「実体が `0.1.127` で配られている」と書いていたが、誤りだった。** `0.1.127` は**リリースの組み立てが失敗して公開されていない**（別の作業で壊れた記述が混ざり、4つの環境すべてでビルドが落ちた）。この `0.1.128` も**タグを打っていない**ので、リリースにはなっていない。**5つが実際に手元へ届いたのは `0.1.129` である。**

- **プレビュー（HTML ／ SVG）も拡大縮小できるようになった。** これまでは「＋」「−」を押しても何も変わらず、生テキストに切り替えたときだけ効いていた。**プレビューは別の文書**なので中の文字には触れず、**ブラウザの拡大と同じように箱ごと**大きくなる（縮めれば1行に多く入る）
- **プレビューの既定も、他と同じだけ小さく始まるようになった。** これまでプレビューだけが縮まずに出ていた
- **プレビューでも「探す」が出るようになった。** 押すと**生テキストに切り替わってから探す**——箱の中は別の文書で、外からは中身に触れられないため。切り替えたことと、「整形して見る」で戻せることが画面に出る
- **Ctrl+G でも探し始められるようになった**（Ctrl+F と同じ）。**窓が開いている間の Ctrl+G は、いままでどおり「次の当たりへ」**。もう一度押すと、打ち直せる状態になる
- **ファイルのタブを並べ替えられるようになった。** **掴んで運ぶ**か、**Ctrl+Shift+← →**。**掴めるのはマウスの主ボタンだけ**なので、中クリックで新しいタブに開こうとして並びが変わることはない。**運ぶのは端で止まる**（移る操作は端で回る）。並べ替えた順は覚えている
- **大きいプレビューでは、探すための切り替えを自動でしない。** 時間がかかることを先に断るので、押すかどうかは決められる（「生テキストで見る」を自分で押すのは今までどおり）
- **画像だけは、これまでどおり倍率で変わらない。** 入れ物の幅まで縮めて出す作りなので、倍率を掛けるとその判断を崩す。画像では「探す」も出ない（文字を持たないため）

## 0.1.127
- **ファイル一覧で、切れた名前の全体が読めるようになった。** 名前に**マウスを乗せる**と全体が出る。フォルダもファイルも同じ
- **指で触る画面では、行を長押しする**と出るメニューの先頭に名前が出る。**幅が狭くていちばん名前が切れるのはスマホ**なので、乗せる操作の無い側にも道を用意した
- 出るのは**名前だけ**で、フルパスではない（パスは今までどおり、行の右のコピーで取れる）
- **切れていない名前でも同じように出る。** 出し分けると、描いたあとに幅を測ることになる
- **上のパンくずは変えていない。** あちらは1つ1つが切れているのではなく、帯ごと溢れているので別の話

## 0.1.126
- **ファイルを何枚でも開けるようになった。** サイドバーで押すたびに**タブが1枚増え**、前に見ていたものは閉じない。`要件.md` と `計画.md` を見比べる、`設計.md` を見ながら `実行レポート.md` を書く、がそのままできる
- **タブは「✕」で1枚ずつ閉じられる。** 閉じたあとは**右隣（無ければ左隣）**へ移る。**最後の1枚を閉じると、列そのものが消える**
- **同じファイルをもう一度押しても増えない**——そのタブへ移るだけ。ただし**ファイルの面へは寄る**ので、セッション側を見ているときに押しても迷子にならない
- **名前が衝突したら、区別が付くまで親のフォルダが付く**（`要件.md` が2枚なら `x/要件.md` と `y/要件.md`）
- **読み込み直すと、開いていたタブが順序ごと戻る。** タブ帯は折り返さず横へスクロールするので、何枚開いてもヘッダは1行のまま
- **中身の文字を、その場で大きく／小さくできるようになった。** 帯の「−」「＋」で段が動き、**間の数字（`100%`）を押すと既定へ戻る**。段はブラウザの拡大率と同じ 80〜200%
- **既定の文字が、これまでの 0.85 倍で始まるようになった**（一度に読める量が増える）。**変えた倍率は覚えていて、別のタブでも同じになる**。効くのは整形した Markdown と生テキストだけで、画像と HTML ／ SVG は変わらない
- **Ctrl+F で、開いているファイルの中だけを探せるようになった。** 当たりは全部に薄い印、**いま見ている1つだけ濃く**なり、`3 / 17` のように何番目かが出る。**Enter ／ Ctrl+G で次へ、Shift 付きで前へ、Esc で閉じる**
- **スマホからも探せる**（🔍 のボタン）。送ると**中身の箱だけが動く**ので、画面全体は動かない
- **ファイルビュアを開いていない画面では、Ctrl+F を奪わない。** いつもどおりブラウザの探索が出る。画像と、HTML ／ SVG を箱で描いているときも奪わない——**「生テキストで見る」に切り替えれば探せる**ことを、そのとき画面で知らせる
- **狭い窓では「生テキストで見る」が印だけになった。** 帯に3つ入ったぶん、いちばん広い文字を落として1行に収めている（**押したときの動きは変わらない**）

## 0.1.125
- **中クリックで、新しいタブに開けるようになった。** 一覧の**カード**・**PJT の枠**・**サイドバーのファイル**のどれもが対象で、**Ctrl／Cmd＋クリック**でも、**Ctrl／Cmd＋Enter** でも同じ。2枚を見比べるのに画面を往復しなくてよくなった
- **いまの操作は1つも変わらない。** 中クリックしても**選択は動かず、画面も切り替わらない**
- **カードの中の操作（鉛筆・ゴミ箱・電源）や、枠の「＋」「×」を中クリックしても開かない。** あれは別の意味を持つ
- **中クリックしたときに、丸いアイコン（ブラウザの自動スクロール）が出ない**——カードと枠では止めてある
- **並べ替えは、マウスの主ボタンでしか始まらなくなった。** これまでは**中ボタンでも右ボタンでも掴めて**、そのまま動かすと並びが変わった。指とペンは今までどおり
- **Shift と Alt の扱いは、サイドバーのファイルだけ違う。** あそこは Shift＋クリックで**新しいウィンドウ**、Alt＋クリックで**保存**（**保存名は `file` になる**——中身をそのまま返す口はファイル名を伝えていない）。カードと枠では効かない。**理由は作りにある**——ファイルの行は本物のリンクにできるが、カードと枠はできないので、ブラウザの作法を借りられない
- **サイドバーのファイルは、`Space` では開かなくなった**（`Enter` は効く）。リンクになったので、リンクの標準に合わせてある。**同じ一覧でも、フォルダの行は今までどおり `Space` で開く**

## 0.1.124
- **枝分かれが、押しても何も起きないまま終わることがなくなった。** 寝ている席から枝を作ると、**起こした直後に `/branch` が消えて**、10分待たされた末に「枝分かれが確かめられませんでした」で終わることがあった。**起こした席は、フックが届いて「入力待ち」に見えても、画面はまだ後処理を描いている**——その最中に書いた文字はどこにも入らない
- **いまは、届いたことを確かめてから確定する。** 静かになるのを待って書き、**本文が画面に現れたのを見てから**確定を送る。現れなければ入力欄を畳んで撃ち直す（3回まで）
- **あなたが入力欄から送る指示は、これまでどおり**（確かめの待ちは入らない。届かなければその場で分かるため）
- **枝分かれの各段が記録に残るようになった。** 失敗したとき、どこまで進んでいたかが `agentdashboard logs` から読める——これまでは「押した」と「終わった」しか残らず、**撃つ前で止まったのか、撃ったが届かなかったのかを区別できなかった**

## 0.1.123
- **PC でも、何かを選んでいる間に別の種類を押したら、選択が解けるだけになった。** カードを選んだ状態で PJT の枠をクリックしても、枠を選んだ状態でカードをクリックしても、**押した相手は選ばれない**。これまでは**もとの選択が消えるのと、押した相手が選ばれるのが同時に起きて**いた——画面は変わらないが、**帯の中身が入れ替わって、押そうとしていたボタンが別のボタンになる**
- **キーボードの Space も同じ**なので、マウスと食い違わない
- **選び直したいときは2回押す。** 指で触る画面なら、**長押しで1回**で選び直せる
- **ダブルクリックと Enter は変わらない**（どちらも開く）。**開いたあとに、身に覚えのない選択が残ることも無くなった**——まとめて選んでいたものも、そのまま残る

## 0.1.122
- **打ち間違えても、近いスラッシュコマンドの候補が出るようになった。** `/coten` で `context`、`/rewnd` で `rewind` のように、**綴りが少しずれていても当たる**
- **語の順番が入れ替わっていても当たる。** `/read_pjt` と打っても `pjt_read` が出る。`-` と `_` の違いも見ない（`/lead-pjt` でも出る）
- **いつもの打ち方は1文字も変わらない。** `/c` や `/con` のように**当たるものが1件でもあるうちは、近いものを探しにいかない**——一覧が並び替わるのではなく、**今日0件だった入力が数件になる**だけ
- **見当外れは出さない。** 2文字以下では緩めず、遠すぎるものは0件のままにしてある。**当てずっぽうが並ぶより、0件のほうがマシ**という判断
- **近いもので当てたときは、どれも選ばれていない状態で開き、「近いものを出しています」と添える。** そのため**素の Enter は今日どおり改行**で、**↑↓ で選べば Enter で確定**、Tab は選ばなくても先頭が入る——本文の途中に `/なにか` と書いて改行したいだけのときに、勝手に補完されない
- **完全に打ったものが、前方一致で先に来るものに負けなくなった。** `/issue_exe` と打ち切っても `issue_exe-phase` が先に選ばれていて、**Enter を押すと違うコマンドが入っていた**のを直した
- **「この PC のコマンドを読めませんでした」が、当たらないたびに出ていたのを直した。** 読めなかったぶんが1件でもある機械では、**当たらない入力のたびに毎回そちらへ落ちていた**。読めなかった件数は下端に出る

## 0.1.121
- **`/login` を打っても候補が1件も出なかったのを直した。** 打てるコマンドなのに一覧に現れない状態で、**打ち間違いの許容とは関係が無い**——組み込みの表に名前が無いものは、そもそも当たりようがなかった
- **よく打つ組み込みコマンドを16件から24件へ増やした。** 足したのは `/login`・`/logout`・`/config`・`/mcp`・`/doctor`・`/hooks`・`/plan`・`/skills`
- **名前は手元の claude の実物と公式の一覧の両方で確かめてある。** 打っても動かない候補は、出ない候補と同じくらい困るため

## 0.1.120
- **何かを選んでいる間は、指で触る画面から画面が飛ばなくなった。** カードを選んだ状態で PJT の枠を押しても、枠を選んだ状態でカードを押しても、**起きるのは選択が解けることだけ**になっている。選択を解こうとして押した場所が遷移の的になっていたのを直したもので、**押した相手が選ばれることもない**
- **開きたいときは2回押す。** 1回目で選択が解け、2回目は「何も選んでいない」ので開く。**何も選んでいなければ、いままでどおり1回で開く**
- **「PJT を追加」で足したのではない枠**（「×」が出ないほう）は、選択に足せないので**押すと選択ごと解ける**。見た目が同じ枠と結果が違う唯一の場所
- **PC は変わっていない**（シングルで選び、ダブルで開く）
- **掴んで運んだあと、同じカードをもう一度押しても開かないことがあったのを直した。** キーボードの Enter でも、マウスの押しでも起きていた

## 0.1.119
- **スラッシュコマンドの候補が、一行目の頭でなくても出るようになった。** 文を書き始めたあとで `/` を打っても一覧が出るので、**名前を思い出すために使える**。空白や改行の直後の `/` が対象で、**`https://example.com/x` や `a/b` のような語の途中では出ない**——URL を貼るたびに一覧が被さらないようにしてある
- **文の途中で候補を決めても、前の文は消えない。** 決めたあとは名前の直後から続きが打てる
- **引数を打ち始めると一覧が閉じるようになった。** 名前が決まったあとは Enter が改行に戻るので、**一覧が Enter を奪っている時間が短くなっている**
- **一行目の頭以外で決めたものは、命令としては実行されない**（claude が命令と読むのは受け取った文の先頭だけ）。名前を引くための道具として使う

## 0.1.118
- **左から出る吹き出しの行間が詰まった。** 機械の発言（コマンドの結果・道具の出力など）は、行と行のあいだが**0.8倍**になり、同じ高さでより多くが読めるようになっている。**あなたの青い吹き出しとファイルの中身は変わらない**
- **本文を畳んだとき、開く前に読んでいた位置へ戻るようになった。** これまでは畳んでも表示位置がその場に留まったので、**本文が消えたぶん下の行が迫り上がってきて、読んでいた行が画面のどこにも無くなっていた**
- **一覧でパスをコピーしたときの「コピーしました」が、4秒で消えるようになった**（これまでは7秒）。**コピーに失敗したときの表示は、これまでどおり自分では消えない**——値を選んで取るための逃げ道を兼ねているため

## 0.1.117
- **指で触る画面で、選んでいないカードの右上に触れただけで作業が止まることがあったのを直した。** 直前の版で操作を「選んだときだけ出す」ようにしたが、**消えていたのは見た目だけで、触れる場所は残っていた**——見えない電源に当たって、走っている claude が黙って止まる形になっていた。電源は指で押しやすいよう当たり判定を広げてあるぶん、なおさら当たりやすかった
- **`agentdashboard state-dir` の断り書きが、人が端末で叩いたときだけ出るようになった。** 機械が読むときは1行だけを返す。**Windows で消す道がこの断りにつまずき、設定で変えた置き場所ではなく既定の場所を消しに行く恐れがあった**（開発者向け）
- テストの中の説明を1つ、実態へ直した（開発者向け）

## 0.1.116
- **構造化ビューの文字が、発言の種類ごとに変わるようになった。** 機械の吹き出しは一段小さく、あなたとアシスタントの発言は一段大きい。**段落と段落のあいだも広がった**ので、段の切れ目が読めるようになっている
- **畳んだ本文の末尾から、水色の膜が消えた。** 畳まれていることは、**薄れていく字と「続きを読む」の帯**が伝える。器の色と食い違って異物に見えることが無くなった
- **「続きを読む」を押しても、読んでいた位置が動かなくなった。** これまでは末尾の近くで押すと、開いた本文の末尾まで一気に運ばれていた。**畳むときも同じ**
- **畳まれた行の要約が、本文より一段沈むようになった。** 畳んだものと本文が同じ明るさで並んでいた
- **PJT 専用画面の区画のあいだに、仕切りの線が立つようになった。** 上下の端は地へ溶ける
- **指で触る画面で、カードの操作が「選んだとき」だけ出るようになった。** これまでは常に出ていたので、触ろうとして押してしまうことがあった。長押しで選べば出る
- **ファイルビュアの「ブラウザで開く」が記号になった。** 隣の「閉じる」と揃えてある
- **「コピーしました」が引っ込むようになった。** これまでは出たまま残り、押し直しても変わったように見えなかった。**失敗したときは消えない**——値を選んで手で取れるようにするため
- **版のドロップダウンが、新しい版から並ぶようになった。** 何が並ぶのかも書いてある——**並ぶのは保管庫にある版だけ**で、走っている版がそこに無いことがある
- **「最新です」が、何と比べたのかを名乗るようになった。** ディスクに新しい版があるときは出ない。同じ1枚の中で上と下が逆のことを言わなくなった
- **`agentdashboard state-dir` が、答えた場所は「そのツリーの既定値」だと断るようになった。** 走っているインスタンスが別の場所を使っていると、黙って別の場所を答えていた（開発者向け）
- **入力欄で当たる候補が0件のとき、↑↓ が入力欄の行移動へ戻るようになった。**

## 0.1.115
- **入力欄で `/` を打つと、打てるスラッシュコマンドの候補が出るようになった。** これまでは名前を覚えていないと使えなかった。↑↓ で選び、**Enter か Tab で決まる**——候補が出ていないときの Enter は、これまでどおり改行のまま。`Ctrl+Enter` が送信なのも変わらない
- 集めるのは**その PC にあるもの**（PJT とあなたのコマンド・スキル・プラグイン・組み込み）。**別の PC のものは混ざらない**
- **一覧が全部でないことは、隠さずに出す。** 8件を超えたぶんは数で言い、読めなかったフォルダがあればその数も出る。**MCP のプロンプトはここに出ない**ことと、組み込みの表が版とずれうることは常に書いてある

## 0.1.114
- **サブエージェントの完了を待ったまま、カードが解けなくなることがあったのを直した。** セッション自身が「フォークは全部終わった」と言っている画面でも、「サブ待ち」が張り付いて戻らなかった。**走っているかどうかは、一覧の時計が進んだかで判定する**——終わったサブエージェントも一覧に残り続けるので、行が在ることは走っている証拠にならない
- **触る端末（スマホ・タブレット）で、ターミナルの文字をその場で選べるようになった。** これまでは長押しで文字だけの画面へ移ってから選ぶ必要があった。長押し・つまみ・OS のコピーのメニューまで、ブラウザが普段どおりに出す
- **`/branch` で会話が別の席へ移ったとき、見ていた画面が付いて行くようになった。** 別の端末で枝分かれを押されると、見ているだけの側は何もしていないのに会話がすり替わっていた。**一言出してから移る**（承認は求めない）。自分で開いた枝は飛ばさない
- **サブエージェントの吹き出しの地を、もう少し明るい黄色にした。**

## 0.1.113
- **機械が積んだ待ちのメッセージが、人の発言の顔で出なくなった。** サブエージェントの報告や他セッションからの連絡が作業中に届くと、これまでは**灰色の吹き出しで右側（利用者の側）**に、生のタグのまま並んでいた
- **左から、種別ごとの色で出る。** 名乗り・包み剥がし・畳みも、他の機械の発言と同じ扱いになる
- **人が作業中に送った指示は、これまでどおり右のまま。** 見分けは「本文が丸ごと包みか」で、**前後に1文字でも別の字があれば人の側**へ倒す——包みを引用しただけの文は巻き込まない

## 0.1.112
- **送った直後の取り消し（`↑`）が、ターミナルをクリックしなくても効くようになった。** これまでは文を書いている時点で焦点がアプリの入力欄に在るため、一度ターミナルを触って焦点を移さないと取り消せなかった
- **取り消した文と画像が、アプリの入力欄へ戻るようになった。** これまでは CLI の作法どおりターミナル側の入力欄にだけ戻っていたので、アプリから送った人は戻り先が食い違っていた。**画像は選び直さずにそのまま送り直せる**
- **既に読み込まれたあとに `↑` を押しても、文は戻らない。** 取り消しは起きていないのに戻ると、取り消せたと誤解するため
- **複数行を書いている最中の `↑` は、これまでどおり行の移動である。** 取り消しに使うのは「送った直後で、入力欄が空のとき」だけ

## 0.1.111
- 右クリックの「絶対パスをコピー」が、写っていないのに「コピーしました」と出ていたのを直した。素の HTTP（スマホ・LAN）で、メニューの焦点の檻に選択を奪われていた

## 0.1.110

### 見た目

- **機械の吹き出しの2色を、明るく鮮やかにした。** サブエージェントの報告は
  くすんだ茶に見えていたのを、**橙がはっきり出る色**へ戻した。他セッションからの
  連絡は**わずかに白を足して**やわらげた。
- **その他の機械（差し込まれた文など）の琥珀と、人の青は変えていない。**

## 0.1.109

### 直したところ

- **枝分かれで、元の会話が席を失うことがあったのを直した。** `/branch` は効いていたのに
  「確かめられなかった」と判断して**呼び戻さずに終わる**ことがあり、そのとき元の会話は
  どの席にも居ない状態になっていた。**確かめられなかったことは、起きていないことの
  証拠にならない**ので、記録を引き直してから判断する形にした。
- **本当に起きていなかったときは、送った `/branch` を入力欄から消す。** 残しておくと、
  諦めたあとに**遅れて効いて**、同じ事故を起こす。
- **席を失っても戻れるようにした。** 枝がカードを乗っ取ると元の会話が記録から引けなくなり、
  「もう一度呼び戻す」も `session branch` の相方も「見つかりません」で終わっていた。
  **枝の印から辿って**呼び戻せる。
- 枝分かれが**ログに跡を残す**ようにした。どの段まで進んでいたかが後から読める。

## 0.1.108

### 直したところ

- **サブエージェントの報告と、他セッションからの連絡の地の色を、わずかに温かくした。**
  黄みと白みを足してほしい、という指定によるもの。**明るさは変えていない**——足すと
  文字が読みにくくなるので、**足したぶんだけ暗くし直している**。見た目としては、
  同じ暗さのまま**色だけが柔らかくなる**。
- 見分けやすさは前と同じに保った。**吹き出しの中のコードの地**（吹き出しより明るい）が
  先に読みにくくなるので、そちらを基準に決めている。

## 0.1.107

### 直したところ

- **スマホで、端末の文字を普段どおり選んでコピーできるようになった。** これまでは
  長押しすると**1行に灰色の帯が付くだけ**で、範囲は伸ばせず、コピーのメニューも
  出なかった。長押しすると**文字だけを並べた面**が開くようになり、そこでは長押し・
  選択ハンドル・コピーのメニューが**いつもどおり全部使える**。
- **面には遡ったぶんも並ぶ**ので、画面から流れていった行もそのまま選べる。触った行に
  印が付き、開いた瞬間にそこが見えている。まとめて取りたいときは「全部コピー」。
- 閉じるのは「閉じる」か Esc。**端末そのものの触り方は何も変わっていない**——
  入力欄をタップすればこれまでどおりキーボードが出るし、指でなぞる遡りもそのまま。

## 0.1.106

### 直したところ

- **構造化ビューが「いちばん下まで送れない」と言われたときに、原因を追える材料が
  残るようになった。** これまでは、その症状が起きても**記録に何も残らなかった**ので、
  あとから追いようが無かった。**画面の見え方そのものは変わらない。**

## 0.1.105

### 直したところ

- **構造化ビューに画像を添えると、横へスクロールできてしまっていたのを直した。**
  画像だけが**入れ物より 24px はみ出して**いたので、下端に横スクロールバーが立ち、
  読むのに関係のない横移動ができる状態だった。**絵の幅が入れ物に収まる**ようになり、
  横のスクロールバーは出なくなる。

## 0.1.104

### 直したところ

- **セッションの上に出る断りが、どれも同じように振る舞うようになった。** それまでは
  「復旧に失敗した」「セッションが見つかりません」「端末を開けません」「枝分かれに
  失敗した」の4つだけ**出したまま消えず**、逆にモードやモデルの切替の断りは
  **5秒でベルからも消えていた**——読める断りは消えず、消える断りは読めない、という
  形だった。**いまはどれも5秒で上の行から下り、ベルには残る。** ベルを押せば時刻つきで
  読み返せる（20件まで）。枝分かれの断りに付く「もう一度呼び戻す」も、これまでどおり
  ベルから押せる。
- 断りが行から下りたときに、**ベルが右から左へ飛ばなくなった。**

## 0.1.103

- 帯のアイコンボタン（サイドバー・設定・アプリのベル・LAN）を 48px から 40px へ縮めた。
  1.5倍は実物の帯で主張が強すぎたため。絵も比を保って 24px → 20px。

## 0.1.102

### 直したところ

- **枝分かれの並びが想定どおりになった。** 押すと**元のセッションはその場に残り、
  枝はその1つ右隣**に入る。それまでは**2枚とも枠の右端へ移動し、しかも左右が逆**
  だった——並べ替えの基準を「呼び戻した席」にしていたためで、あれは新しいカードなので
  必ず枠の末尾に付く。**基準を「押した席」へ移した**（押した席は動いていないので、
  そこがそのまま元居た場所になる）
- **別の PC のセッションでも並びが直るようになった。** 呼び戻した席が**枠に載る前**に
  並べ替えていたので、記録の側が「知らないカード」として断り、**席はあるのに並びだけ
  直らない**ことがあった。載るのを待ってから並べ替える

## 0.1.101

### 直したところ

- **API のエラーを赤くして、器に薄い地を1枚持たせた。** 利用者の「こういうエラー系は
  赤文字にしてもう少し目立たせて」に応えたもの。**見分けは記録の名乗り**
  （`isApiErrorMessage`）だけで行う——`Request timed out` と `No response requested.`
  は画面上まったく同じ姿で並び、**違うのは欄だけ**なので、字面で判定すると
  **エラーでない短い返事まで赤くなる**
- **読まれる前に取り消された発言が、取り消されたと分かるようになった。** 送った直後に
  止めても「送信済みのまま」に見えていた。**合図は中断マーカーが
  `interruptedMessageId` を持たないこと**で、あれは止められたアシスタントの
  メッセージ ID なので、**持っていれば読まれた後**である。**消さずに、地は「待ち」と
  同じにして打ち消し線で示す**——記録は残す

## 0.1.100

### 直したところ
- **サブエージェントが走っている最中に、カードが「入力待ち」になることがあったのを
  直しました。** メインが動き出す瞬間、画面から一覧が消えてスピナーが出るまでの
  ほんの一瞬だけ「誰も動いていない」ように見えていました。**一覧が続けて2回
  見えないことを確かめてから**入力待ちへ戻すようにしています——サブが終わって
  入力待ちに戻るまでが最大5秒ほど遅くなりますが、走っているのに終わったように
  見えるよりは軽い、という判断です

## 0.1.99

### 見た目が変わったところ
- **上のバーのボタンが3つとも大きくなりました**（1.5倍）。ベル・IP・設定です。
  **指で押す画面で狙いやすくする**ためで、サイドバーの開け閉めボタンと同じ大きさに
  揃えています
- **アドレスをコピーするボタンの絵に「IP」が入りました。** これまでは素のコピーの絵
  だったので、**上のバーには写す操作が他にもあるなかで、何を写すボタンなのかが
  分かりませんでした**

## 0.1.98

### 見た目が変わったところ
- **横に長いコードのスクロールバーが、参考どおりの太さになりました。** 前の版で
  「細くしました」と書きましたが、**指定していた太さは1度も効いていませんでした**
  ——ブラウザが、細さの指定と寸法の指定を同時に受け取ると**寸法のほうを捨てる**
  作りだったためです。書き方を変えて、狙った太さがそのまま出るようにしました。

### 直したところ
- **つながりが切れているときに送ろうとすると、その旨が出るようになりました。**
  これまでは**押しても何も起きない**だけで、送れていないことが画面のどこにも
  出ていませんでした。**打った文はそのまま残る**ので、つながり直してから
  送り直せます。

## 0.1.97

### 見た目が変わったところ
- **人が打っていないメッセージの吹き出しが、鮮やかになりました。** これまでは濁った
  茶色に見えていました——**彩度が、あなたの発言の青の56%しかなかった**ためです。
  琥珀という役割はそのままに、濁りだけを取りました。
- **人が打っていないメッセージだけ、吹き出しが少し広くなりました**（1.1倍）。
  あなたの発言の幅は変えていません。
- **畳んだものを開いても、吹き出しの色が変わらなくなりました。** 左に出ていた
  水色の縦線も消しました。**開いている印は、行の記号（`⌄`）の色が変わることで
  分かります。**
- **コードの見せ方を整えました。** インラインコードとコードブロックが同じ地になり、
  角が丸くなって余白が広がりました。**横に長いコードのスクロールバーが細くなり、
  邪魔をしなくなります。**

## 0.1.96

### 使いやすくなったところ
- **スマホで、知らせの帯を指で払って消せるようになりました。** 上・左・右のどちらへ
  払っても消えます。**下へは払えません**——あそこはブラウザの「引き下げて読み直す」が
  持っている場所なので、取り合うと消すつもりでページごと読み直されてしまいます。
- **払って消しても、ベルには残ります。** 消すのは「いま読んだ」という意思表示で、
  捨てる操作ではありません。
- **帯を押さえている間は、消えるまでの時間が止まります。** これまで時間を止める手は
  マウスを乗せることだけで、**指の画面には手がありませんでした**。読み終わるまで
  押さえておけます。

**これまでの消し方はそのままです。** 7秒で消えることも、✕ を押して閉じることも
変わりません。**パソコンでは払えません**——文言を選んで写せなくなるためで、
あちらは ✕ が狙いやすく、マウスを乗せれば時間も止まります。

## 0.1.95

### 見やすくなったところ
- **機械が入れたメッセージから、意味の無い記号を取り除きました。** サブエージェント
  の報告や他のセッションからの連絡には、`<task-notification>` のような**内部の覚え書き
  がそのまま出ていました**。いちばん場所を取っていたのは、**毎回まったく同じ文言が
  入る欄**です（読んでも何も分からないもの）。いまは「完了」の印とエージェントの名前、
  そして**報告の中身だけ**が出ます。
- **セッションを引き継ぐときの「会話履歴の丸写し」を、3行まで畳むようにしました。**
  実際に数えたところ、**機械が入れたメッセージの4分の3がこれ**でした。中身は過去の
  やりとりの写しで、**機械が文脈のために入れたもの**なので普段読む必要がありません。
  読みたいときは押せば開きます。
- あわせて、**フックからの知らせ**と**ローカルコマンドの断り書き**からも、毎回同じ
  文言を落としました。

**画面から消えるだけで、記録は1文字も減っていません。** 押せば元のまま読めます。

## 0.1.94

### 直したところ
- **サブエージェントが走っているのに「サブ待ち」にならなかったのを直しました。**
  待ちが少し長引くと、**カードは「作業中」や「入力待ち」のまま**でした。画面の
  いちばん下に出ているサブエージェントの一覧を読んで決めているのですが、**その一覧が
  見えていませんでした**——一覧はサブが立った瞬間に一度描かれたきり動かないので、
  端末の新しいところだけを見ていると窓の外へ落ちてしまいます
- **サブが手を動かしただけで「作業中」に見えていたのを直しました。** サブが道具を
  使うたびに、あなたが何もしていなくてもカードが作業中に変わっていました。**メインが
  走っているかどうかは画面のスピナーで見分ける**ようにしたので、手が空いていれば
  「サブ待ち」（☕）が出ます

## 0.1.93

### 増えたところ
- **打ったスラッシュコマンドを押すと、そのコマンドの中身がその場で開きます。**
  `/pjt_read` のように打った行が**押せる見た目**（水色・少し太字）になり、押すと
  **ファイルに書かれている内容**がカードで出ます。別の画面へ飛びません
- **フロントマター（`---` で囲まれた設定）も読める形で出ます。** 項目ごとに名前と
  値を並べ、**値が4行以上のものは畳んで**あります（押すと開きます）——`description`
  に長い説明を書いたスキルでも、カードが説明で埋まりません
- 探す順序は Claude Code と同じです（この PJT のコマンド → 利用者のコマンド →
  この PJT のスキル → 利用者のスキル）。**見つからないときは、探した場所を並べて
  断ります**

### 変えたところ
- **打った形が、吹き出しの本文から独立しました。** これまでは本文の頭に混ざって
  いたので、**畳むと隠れることがありました**。いつでも見えます

## 0.1.92

### 直したところ
- **スマホでコピーを押すと、知らせが縦に1文字ずつ改行されていたのを直しました。**
  上のバーは名前・ベル・設定などが横幅を取り合う場所で、そこへ文を置いていたため
  **押し出されて幅が1文字ぶんまで縮んで**いました。**バーの外へ浮かせた**ので、
  どれだけ混んでいても潰れません
- **写せなかったときに出る値も同じように浮きます。** あちらは**選んで取るための
  逃げ道**なので、潰れると取りようが無くなります

### 変えたところ
- **コピーを押すと、候補を全部まとめて写します**（複数あるときは改行で区切って）。
  貼った先では**各行がそれぞれリンクになる**ので、受け取った側が上から試せます
  - **「他の候補」のボタンは無くなりました。** 選ばせる相手が居なくなったのと、
    上のバーは場所を取り合うためです。**何が入るかはボタンにマウスを乗せると読めます**
  - 何本入ったかは知らせに出ます（「2件ぜんぶコピーしました（改行区切り）」）
  - **1本に絞らないのは、確実な1本が選べないから**です。いま繋がっているアドレスは
    **スマホで開いたときにしか分からず**、PC で押すと推定しか残りません。
    そして**どれが届くかは、貼った先で開くまで分かりません**

## 0.1.91

### 直したところ
- **サブエージェントが走っているのに「入力待ち」「作業中」と出ていたのを直しました。**
  カードが**サブ待ち（☕）**になります。前の版では、まれにしか出ていませんでした。
  - 原因は、走っている本数の数え方でした。**サブがまだ生きているうちに「終わった」と
    届く**ことがあり、そのとき数は 0 に戻ります。**待ちが長いときほど外す**という、
    いちばん困る形の外し方でした
  - そこで数え方をやめ、**端末の下に出ている「走っているサブエージェントの一覧」**を
    見て決めるようにしました。一覧が消えれば入力待ちへ戻ります
  - 見に行くのは**5秒に1回まで**で、しかも**端末に何も増えていなければ見に行きません**。
    開いたまま放置したカードが増えても重くなりません
  - **切り替わるまで最大5秒**かかります。それ以上待っても変わらないときは、端末の下に
    一覧（`◯ …`）が出ているかを見てください——出ていなければ、こちらの読み違いでは
    ありません

## 0.1.90

### 増えたところ
- **同じ Wi-Fi の別の端末から開くアドレスが、ヘッダのボタン1つで手に入るようになりました。**
  押すと `http://192.168.0.12:8787/` の形でクリップボードに入るので、そのまま Discord などへ
  貼って、**スマホでタップして開けます**。これまでは自分で番号を調べる必要がありました。
  - **届かない番号は渡しません。** WSL の中から見える番号は**外の端末には届かない**のに
    `192.168.` で始まるので本物に見えます。横流し用の仮想スイッチや、線が繋がっていない
    ものも落とします
  - **いま開いているアドレスが先頭に出ます。** それは推定ではなく、**あなたの端末が現に
    繋がっている先**だからです。こちらが数えた番号と食い違ったときは**両方出します**
    ——どちらが当たりかは、渡した先で開くまで分からないためです
  - 待ち受けを広げていない（`bind_addr` が `127.0.0.1`）ときは、**ボタンの代わりに
    広げ方の案内**が出ます。押しても死んだアドレスしか渡らないボタンは置きません
  - 写せない環境では、**値を選べる形で画面に出ます**（折り返して最後まで見えます）
  - 開いた先では**合言葉（LAN パスワード）を聞かれます**。その旨も知らせに出ます
  - CLI からは `agentdashboard address` で同じことができます（`--json` で全候補）

## 0.1.89

### 増えたところ
- **会話を枝分かれさせるボタンが、PJT 専用画面のセッションに1つ増えました。**
  押すと、その会話から枝分かれした新しいセッションができ、**元の会話は隣の席に残ります**
  ——これまでは端末へ `/branch` を打つしかなく、**打った席がそのまま枝になる**ので、
  元へ戻るには画面に出た長いIDを目で読んで写す必要がありました。**IDは1文字も触りません。**
  - 並びは**左が枝、右が元**です
  - どちらが枝かは**札**で分かります（名前は、あなたが名付けた会話に付いて回ります）
  - **セッション専用画面には出ません**（あちらには「左隣」が無いため）
  - **まだ1ターンも会話していない席では押せません**（CLI 側が断るため。理由が出ます）
  - CLI からは `agentdashboard session branch <カードID>` で同じことができます
  - 途中で失敗して元の会話が席を失った場合は、**断りの中の「もう一度呼び戻す」**で戻せます

## 0.1.88

### 変えたところ
- **サイドバーの「1つ上へ」が、押せるボタンだと分かる見た目になりました。**
  これまでは細い山形（`^`）だったので、**パスの区切りや文字に紛れて**どこを押せば
  上へ行けるのか分かりにくい状態でした。**軸のある太い矢印**にし、**枠**を付けています

## 0.1.87

### 変えたところ
- **自分の発言の吹き出しが、少し細くなりました。** 短い返事（「はい」など）でも
  横に間延びしないよう、**下限をひらがな25文字ぶんから20文字ぶんへ**縮めています。
  長い発言の見え方は変わりません

## 0.1.86

### 変えたところ
- **画面のいちばん上に居座っていた知らせが、最前面のトーストへ移りました。**
  左から出て**7秒ほどで自分から消え**、消えるまでの残り時間が**ゲージ**で読めます。
  **場所を押しのけないので、出ている間も本体が下がりません**——狭い画面ほど効きます。
  **マウスを乗せている間は止まる**ので、読んでいる最中に消えることはありません
- **消えた知らせは、ヘッダの新しいベルから読めます。** 未読の数がバッジに出て、
  開いた瞬間に既読になります。1件ずつ消すことも、全部消すこともできます
- **ベルの中身は端末をまたぎます。** PC で見た知らせがスマホのベルにも入っています
  （**記録に残す**ようになったためです）。ただし**線が切れた・起こせなかった**の
  ような「その端末で起きたこと」は、これまでどおり読み込み直すと消えます
- **上に残る知らせもあります。** 構造化ビューの縮退・サーバ同士の連絡断・
  版が変わった、の3つは**消してはいけない**ので帯のままです——出ていること自体が
  知らせで、消すと「いま壊れている」ことが読めなくなります
- **動きを止めていても知らせは届きます。** 「控えめ」「静止」では飛び出しと
  ゲージが止まりますが、**消えるまでの時間は変わりません**（ベルに残るので
  読み落としません）

### 足したところ
- **`agentdashboard notice` で、ベルの中身を CLI からも読めます。**
  `notice ls`（新しい順・未読に印）／`notice read`（まとめて既読）／
  `notice rm [ID]`（1件、または全部）

### 設定
- `notice_retention_days`（既定 30）と `notice_max_rows`（既定 200・アカウントごと）を
  足しました。**知らせが際限なく溜まらないよう、1時間ごとに掃きます**

**記録の形が変わりました**（表を1つ足しています）。**0.1.85 以前へは画面から戻れません。**

## 0.1.85

### 直したところ
- **サブエージェントが走っている間、カードが「サブ待ち」と出るようになりました。**
  これまでは「作業中」のままで、**0.1.79 で入れたはずの「サブ待ち」が一度も画面に
  出ていませんでした**。メインが手を止めてサブ待ちになった直後に、**サブエージェント
  自身のツール実行**で「作業中」へ引き戻されていたためです。**サブ待ちから戻るのは、
  あなたが指示を打ったときと、最後のサブエージェントが終わったときだけ**になりました

## 0.1.84

### 変えたところ
- **左のフォルダ（サイドバー）が使いやすくなりました。** 開け閉めのボタンが**1.5倍**に
  なり、パスの左に**「1つ上へ」**が付きました（PJT のルートより上へは出られないので、
  そこでは押せません）。各行の「コピー」は**絵**になり、**右クリック**（指で触る画面は
  長押し）で**絶対パスをコピー**できるメニューが出ます。押した結果の
  「コピーしました」「コピーできません」は**文字のまま**です——絵にすると成功と失敗が
  見分けられなくなるためです
- **ファイルを読む面から「パスをコピー」が無くなりました。** 写す道は左のフォルダ側に
  1つへまとまりました。**何からの相対パスなのかは、マウスを乗せたときの説明に出ます**
- **Markdown の見出しが大きくなりました。** h1・h2・h3 の段差が字の大きさから読めます。
  同じ整形を使っている**セッションの履歴の見出しも同じ大きさ**になります
- **狭い窓では、どちらの画面でもファイルが画面いっぱいに開きます。** これまで PJT 専用
  画面では幅が固定で、画面からはみ出して横に払わないと読めませんでした
- **「閉じる」が全部 ✕ に揃いました。** ただし**ダイアログの選択肢の「やめる」は文字の
  まま**です——面を閉じるのはいつでもやり直せますが、操作をやめるのは選択の1つなので、
  取り返しの付かなさが違うものを同じ形にしていません
- **「設定」が歯車の絵になりました。**
- **端末をタップしてもキーボードが出なくなりました。** 出すのは**「キーボード」ボタンを
  押したときだけ**です。焦点も渡さないので、タップしただけでカーソルが出ることもありません
- **タブの名前の区切りが縦棒になりました**（`faix | AgentDashboard`）。ダッシュは幅を
  取るうえ PJT 名に混ざる `-` と紛れるので、字面の細いほうへ変えています

### 知っておくとよいこと
- **ターンの終わりに、サブエージェントの本数と行き先がログへ残るようになりました。**
  「サブが動いているのに入力待ちのまま」を追うときの手掛かりです（画面の見た目は
  変わりません）
- **記録の形は変わっていません。** この版から前の版へ戻せます

## 0.1.83

### 変えたところ
- **まだ読まれていない追加メッセージが、自分の吹き出しで出るようになりました。**
  これまでは「待機中」という見出しの行に本文の先頭1行だけが出ていましたが、
  いまは**送った文がそのまま吹き出しに入って全部読めます**。長い文は
  「続きを読む」で開けます——読まれたあとの吹き出しとまったく同じ開き方です
- **待っているあいだは、いちばん下にまとまって並びます。** 待っている最中に
  Claude 側の返事が届いたら、**それが待っているものの上に入ります**。
  これまでは届いた順だったので、あとから来た返事が待ちの下に付いていました
- **読まれると、そのまま青い吹き出しになって本来の位置に移ります。** 何件かまとめて
  送って1件だけ読まれたときも、読まれたものだけが移り、**残りは下で順番のまま待ちます**

### 知っておくとよいこと
- **待っているものは、色で見分けます。** 読まれた自分の発言は青、まだ読まれていない
  ものは**その青から色味を抜いた灰色寄りの青**です。「待機中」という文字は出ません
- 待ちが4件以上たまったときに3件だけ出す動きは、これまでどおりです。
  「ほか N 件が待っています」の行も、待っている塊と一緒にいちばん下に出ます

## 0.1.82

### 変えたところ
- **作業中のセッションへ送った追加メッセージが、送った時点で見えるようになりました。**
  これまでは Claude Code の待ち行列に入っている間ずっと画面に何も出ず、読まれるまで
  届いたのかどうか分かりませんでした。いまは「待機中」の行として本文の先頭1行つきで
  出て、**読まれると消えて、本物のやりとりが並びます**
- **同じ文が2つ並ぶことはありません。** 読まれた時点で待ちの行は畳まれるので、
  送った文が画面に2回出ることはありません。取り消したものも残りません
- **待ちが続いても画面は埋まりません。** 4件以上たまったときは3件だけ出して、
  残りは「ほか N 件が待っています」とだけ言います

### 知っておくとよいこと
- 待機中の行は**吹き出しになりません**。記録が「誰が書いたか」を名乗っていないので、
  人の発言と同じ見た目にはしていません。読まれたときに、あらためて発言として並びます
- **機械が差し込むもの**（サブエージェントの完了通知など）も同じように出ますが、
  読まれるとすぐ消えるので、同時に見えるのはその瞬間に待っている数だけです

## 0.1.81

### 変えたところ
- **端まで運ぶと、画面が自動で送られるようになりました。** 一覧は縦に、PJT 専用画面の
  横並びは横に。指（マウス）を端で止めておくと送り続け、隣より遠くへも運べます。
  ホイールで送っても落とし先は指の下に追いつきます
- **離すと、掴んでいたものはバネで枠へ収まります。** 離した瞬間の速さを引き継ぐので、
  勢いよく離せばそのぶん流れ込みます。押しのけられる側の滑りも、助走のある曲線に
  変えました（200ms）。動かしている間は速さに応じて少し傾きます
- **並べ替えを断られたときは、元の並びへ滑って戻ります。** 返事が来ないまま2秒経った
  ときも同じです
- **カードを選んでいても、枠の押し方は変わらなくなりました**（逆も同じ）。選択モードは
  「同じ種類のもの」の中だけに効きます。記録を持たない枠は、選択モード中でもタップで
  開きます。選ばれているカードを長押しして掴んでも、選択が外れません
- **キーボードとボタンでも並べ替えられます。** カードや枠を Space で選ぶと、上の帯に
  「前へ／後ろへ」（枠は「上へ／下へ」）が出ます。Enter は今までどおり開きます。
  枠にも Tab で止まれるようになりました。動かした結果は読み上げにも出ます
- **「静止」とOSの「動きを減らす」では、バネも滑りも無く、その場で収まります**
  （指への追従だけは残ります）

### 決めていないこと（実機で見て決めます）
- 傾きの強さ、バネの弾み、端で送る速さ

## 0.1.80

### 変えたところ
- **並べ替えの手触りを作り直しました。** 掴んだカード・枠・区画は指（マウス）に
  張り付いて動き、押しのけられる側は隣の1枚ずつ滑ります。境界の上で指を止めても
  行ったり来たりしません
- **右（下）へ運んでも、掴みが途中で解けなくなりました。** 1回動かすと離れて
  しまっていたのは、並べ替えのたびに掴んでいるカードそのものが作り直されていた
  ためです
- **承認待ちのカードがあるときのカクつきを直しました。** 運んでいる間は効果線を
  引き直さず、離してから1回だけ引き直します
- **枠と区画は、掴むと少し縮みます。** 同じ大きさのまま傾くと角がはみ出て見づらかった
  ためです（カードは今までどおり少し持ち上がります）
- **離した瞬間に元の並びへ戻って跳ぶことが無くなりました。** サーバの返事が来るまで
  手元の並びを見せ続けます

### まだ入っていないもの（次の版で）
- 端まで運んだときの自動スクロール、落とすときのバネ、帯からの「前へ／後ろへ」、
  選択モードの種類ごとの分離

## 0.1.79

### 変えたところ
- **サブエージェントが走っている間は「サブ待ち」と出るようになりました。** これまでは
  「入力待ち」と出ていて、**一覧を流し見したときに「終わった」と読めて**いました。
  指示を送れることは変わりません——**色が「まだ終わっていない」を、ゆっくりした明滅が
  「あなたも入力してよい」を伝えます**。印は ☕（メインが一服している、の意）。
  サブエージェントが全部終わると、これまでどおり「入力待ち」へ戻ります

### 直したところ
- **PC でカードを押しても「選ぶ」「開く」が効かなくなっていた 0.1.78 の退行を直しました。**
  掴んで運ぶ仕組みが押した瞬間にポインタを握ってしまい、続くクリックがカードの中身へ
  届いていませんでした。**並べ替えはそのまま使えます**

## 0.1.78

### 変えたところ
- **枠とカードは、掴み手ではなく本体をつまんで並べ替えるようになりました。** 余白でも
  カードそのものでも、押して動かせばそのまま運べます（**PJT 専用画面の横並びは
  これまでどおり左上の掴み手から**）
- **スマホは長押ししてから運びます。** ホーム画面でアプリを並べ替えるのと同じ形です。
  **そのままなぞれば、これまでどおり縦にスクロール**します
- **並べ替えが滑らかになりました。** 持っているものだけでなく、**押しのけられる側も
  滑って動きます**。動きが気になる場合は、設定の「静けさ」を「静止」にすると止まります
- **選んだ枠とカードが、地の色で分かるようになりました。** これまでは小さな白い点が
  付くだけで、しかも**復旧ボタンが出ているカードでは点がボタンの下に隠れて見えて
  いませんでした**。点はやめて、面ごと色が変わります

- **セッションに自分で名前を付けられるようになりました。** カードにマウスを乗せると
  左上に鉛筆が出ます（**スマホは長押しでカードを選ぶと出ます**）。押してその場で書き、
  Enter で決まります。空にすると消えます
- **付けた名前は、`/rewind` などで別のセッションへ乗り換えても付いてきます。**
  名前はカードではなく**会話そのもの**に付くので、同じ会話を別のカードで開いても
  同じ名前が出ます。**Claude が付ける名前は書き換わりません**（自分で付けていない
  カードは、これまでどおりそちらが薄く出ます）
- **一度外したセッションを、あとから呼び戻せるようになりました。** 枠の「＋」を押すと
  「どれを」の欄が出て、その PJT で過去に使ったセッションを選べます。**名前を付けた
  ものは消えません**——付けていないものは最近のぶんだけが並びます
- **もう履歴が残っていないセッションは、選択肢から消えます。** ただし**その PC が
  繋がっていないときは消しません**——見えないだけかもしれないので、「（未確認）」と
  付けたうえで選べるようにしてあります

### 分かっていること（まだ直っていません）
- **走っているセッションが「入力待ち」と表示されることがあります。** 端末は動いて
  いるのにカードだけが戻ってしまう状態で、**原因は追跡中**です。**指示を送れば直ります**
  （次のフックでカードが作業中へ戻ります）。倒れた瞬間の記録が残っていなかったので、
  この版から**追える材料をログに残す**ようにしました

### 直したところ
- **過去のセッションを呼び戻したとき、権限モードが指定どおりに起動していなかったのを
  直しました。** カードの表示は指定した権限モードなのに、**実際には既定で起動して
  いました**
- **スマホでカードを長押しすると、カードではなく PJT の枠が選ばれていたのを直しました**
- **掴んだカードが傾いていなかったのを直しました。** 傾ける指定は書かれていたのですが、
  画面には一度も出ていませんでした

## 0.1.77

### 直したところ
- **スマホから画像を添付して送ると「Failed to fetch」になっていたのを直しました。**
  2枚以上付けたときや、付けてから送るまでに時間が空いたときに起きていました。
  付けた時点で画像の中身を控えるようにしたので、元の画像が入れ替わっても送れます
- **読めない画像を付けたとき、送信を押すまで分からなかったのを直しました。**
  付けた時点で「読めませんでした。もう一度選び直してください」と出ます
- **通信が届かなかったときに、英語のまま「Failed to fetch」と出ていたのを直しました**
- **カードが停滞のまま戻らなくなることがあったのを直しました。** ツール結果の続き行を
  「走っている印」と読み違えていました
- **版を入れ替えたあと、終わったセッションのプロセスが引き取られずに溜まっていたのを
  直しました。** 実機で 78 体溜まっていました

### 変えたところ
- **サイドバーで掘っていたフォルダと、開いていたファイルを覚えるようにしました。**
  読み込み直しても同じ場所へ戻ります
- **セッションに自分で名前を付けられるようにしました。**
  `agentdashboard session nickname <ID> <名前>`（`--clear` で消す）
- **引き取れていない子プロセスの数を、ログ・CLI・画面に出すようにしました。**
  3日間どこにも出ずに溜まっていたのが、今回の発端でした

### 分かったこと
- **スマホのキーボード上部にあるクリップボードの面からは、画像を貼り付けられません。**
  Chrome 側の制限で、こちらでは直せません。**入力欄を長押しして出る「貼り付け」**を
  使ってください（「＋」からの選択も使えます）

## 0.1.76

### 直したところ
- **起こした直後のカードが、一瞬だけ一覧から消えていたのを直しました。** 見た目には
  ほとんど分かりませんが、その一瞬に触ろうとすると空振りしていました

## 0.1.75

### 変えたところ
- **枠とカードを掴んで並べ替えられるようになりました。** 一覧の枠・一覧のカード・
  PJT 専用画面の区画のどこから並べ替えても、**動くのは同じ並び**なので、
  もう片方にもそのまま出ます
- **押し方が変わりました。** PC は**シングルで選ぶ・ダブルで開く**、
  スマホなど指で触る画面は**シングルで開く・長押しで選ぶ**（1枚選んだあとは
  シングルが「選ぶ」に変わります）。並べ替えようとして画面が変わってしまうのを
  防ぐためです。選択から抜けるには**一覧の地を押す**か **Esc**
- **選んだものを、まとめて起こす・外せるようになりました。** 1枚選んだ時点で
  上に帯が出ます。**電源は止まっているカードだけを起こします**——走っている
  claude を押し間違いで止めないためで、何枚が対象で何枚を飛ばすかは押す前に出ます
- **添付が札の形になりました。** ファイル名が出て、押すと大きく見られます
- **停滞したカードが、自分で入力待ちへ戻るようになりました。** これまでは
  ターンが完了せずに終わったとき（レート制限で止まった場合を含む）、
  端末は打てば動くのに表示だけ「停滞」のまま戻りませんでした

### 直したところ
- **スマホや LAN の IP から開いたとき、画像を選んでも貼っても何も起きなかったのを
  直しました**（素の HTTP で別ホストを開くと、ブラウザの機能が一部使えないため）
- **並べ替えた結果が画面で元へ戻ってしまうのを直しました**
- **選択の帯が出ると、カードを開けなくなるのを直しました**

## 0.1.74

### 変えたところ
- **ターミナルの切替に、オフのときも端末の印が出るようになりました。** これまでは
  入っているときだけ出ていたので、切れていると何の切替か分かりませんでした
- **枠とカードに並び順の列が入りました**

### 注意
- **この版より前へは、画面からは戻れなくなります**（記録の形が変わるため）。
  手で戻す場合は `<state_dir>/version-current` を消してください

## 0.1.73

### 変えたところ
- **セッションの操作（電源・終了・ターミナル・拡大／縮小）が、画面の帯から
  そのセッションの区画の真上へ移りました。** PJT 専用画面とセッション専用画面で
  **同じ操作が同じ場所**にあります
- **画面の行き来が1つのボタン（拡大／縮小）にまとまりました**
- **上の余白を詰めました**
